# Đính chính & cập nhật: nguyên nhân gốc rễ KHÔNG phải Mamba

Ngày: 15/9/2026
Liên quan: `md/14thg9.md` (kết luận ngày 14/9), `md/9thg9.md` (calibration)

File này gồm: (0) log việc đã làm hôm nay, (1) số liệu thực đo, (2) đính chính 3 kết luận **sai**
trong `14thg9.md`, (3) xác nhận 1 kết luận **đúng** nhưng đã vô nghĩa, (4) lý do "chặn HARD LOCK"
≠ "là lỗi", **(4.1) KẾT QUẢ PHÉP THỬ QUYẾT ĐỊNH — BatchNorm1d KHÔNG phải thủ phạm**,
(5–6) hai phát hiện mới mà `14thg9.md` bỏ sót, (7) trả lời trực tiếp câu hỏi gốc rễ,
(8) việc cần làm tiếp, (9) các điểm còn tồn.

> **Tóm tắt 1 dòng:** gốc rễ **không phải Mamba**, cũng **không phải BatchNorm1d**.
> Embedding **có** phân biệt danh tính (Rank-1 = **78.15%** so với mốc ngẫu nhiên 2.90%) — nhưng
> **ngưỡng cosine tuyệt đối là sai công cụ** (TAR@FAR=0.1% chỉ 8.45%). Vấn đề nằm ở
> **LUẬT QUYẾT ĐỊNH**, không phải ở kiến trúc. Xem §4.1 KL5–KL8 và §7.

---

## 0. Đã làm gì hôm nay (changelog)

| # | File | Thay đổi | Vì sao |
|---|---|---|---|
| 1 | `infer.py` | Thêm `FusedBundle` (5 tầng vector), `compute_fused_vector()` trả thêm `visual_plain` + `raw_feat` (**TRƯỚC bnneck**) | Đo được cosine trước/sau BN trên **cùng một truy vấn** — trước đây `debug_vis` là weighted-mean còn `fused` là plain-mean nên so lệch, không kết luận được |
| 2 | `infer.py` | `TwoTierMemoryBank` lưu 5 trường (`visual`, `visual_plain`, `temporal`, `raw`, `fused`) + `raw_score()` / `visual_plain_score()`; gom `coarse/temporal/fine` về một helper `_max_sim()` | So sánh được mọi tầng; giữ tương thích ngược với call 2–3 tham số |
| 3 | `infer.py` | Helper `_log_sim_breakdown()` in breakdown ở 3 chỗ: re-acquire, anti-hijack, last-moment update; cờ `debug_sim` | Gỡ 1 dòng debug cũ chỉ in 3 chỉ số |
| 4 | `infer.py` | Tổng hợp cuối sequence: `Sim PRE-BN` / `Sim POST-BN` / `BN degradation` + verdict; gộp vào `summary_metrics.txt` khi `seq_dir: all` | Trả lời câu hỏi trên toàn bộ dataset, không phải đọc log thủ công |
| 5 | `infer.py` | Dùng `load_checkpoint_verbose()` thay khối `load_state_dict(strict=False)` | Cảnh báo nếu `backbone.*` không được nạp (confound, xem §6) |
| 6 | `model.py` | Hàm mới `load_checkpoint_verbose()`: in `missing` / `unexpected` / `shape mismatch` (gom theo prefix) + cảnh báo nghiêm trọng khi thiếu `backbone.*` | `strict=False` đang âm thầm che giấu key thiếu/lệch shape |
| 7 | `configs/config_colab.yaml` | `infer.num_frames: 8 → 12` (khớp `train.num_frames: 12`) | Lệch số token train/infer bóp nghẹt temporal token (§2.1) |
| 8 | `configs/config_colab.yaml` | Thêm `infer.debug_sim: true`, `eval.space: fused` | Cờ tắt/bật chẩn đoán; chọn không gian chính khi eval |
| 9 | `evaluate_reid.py` | Trích **đồng thời** `fused` + `pre_bn` trong 1 lượt backbone; bảng so sánh Rank-1/Rank-5/mAP/mINP/TAR@FAR + `Δ(pre_bn − fused)` kèm kết luận tự động | Phép thử quyết định: BN có làm mất khả năng phân biệt hay không |
| 10 | `evaluate_reid.py` | Thêm `--space fused\|pre_bn`; `compute_tar_at_far()` nhận threshold ngoài và trả `(tar, far, threshold)`; `evaluation_report.json` thêm `primary_space` + `spaces` (giữ nguyên các khoá phẳng cũ) | Tương thích ngược với `9thg9.md` |
| 11 | `calibrate_threshold.py` | `extract_features()` trích nhiều không gian/1 lượt; calibrate **cả 2 không gian** bằng cùng quy trình Fixed-FAR; in bảng so sánh + `ΔTAR(eval)` | Threshold mỗi không gian phải calibrate riêng mới so sánh được |
| 12 | `calibrate_threshold.py` | `calibrated_threshold.json` thêm `space` + `thresholds: {fused, pre_bn}` (giữ `threshold` = không gian chính); `calibration_report.json` thêm `primary_space` + `spaces` | `evaluate_reid.py --space pre_bn` đọc được threshold tương ứng |
| 13 | `calibrate_threshold.py` | ⚠️ **Đổi tên file plot**: `score_distribution_<space>.png`, `det_curve_<space>.png`, `roc_curve_comparison_<space>.png` khi có >1 không gian | Mỗi không gian 1 bộ đồ thị. **Không còn khớp tên cũ trong `9thg9.md`** |
| 14 | `calibrate_threshold.py` | Dùng `load_checkpoint_verbose()` | Cùng lý do #5 |
| 15 | `configs/config_jetson.yaml` | `infer.num_frames: 8 → 16`; `infer_robustness.num_frames: 8 → 16`; thêm `infer.debug_sim: true` | Cùng lỗi lệch token như #7 |
| 16 | `configs/config_local.yaml` | `infer.num_frames: 8 → 16`; thêm `debug_sim: true` | Cùng lỗi lệch token như #7 |
| 17 | `configs/config_colab.yaml` | `infer_robustness.num_frames: 16 → 12` | Cùng lỗi lệch token như #7; `evaluate_reid_robustness.py` đọc block này qua `cfg.get('infer_robustness')` |
| 18 | `infer.py` | Tách `format_bn_debug_lines()` dùng chung cho per-sequence + `AGGREGATED METRICS`; thêm dòng tách tag + tỉ lệ HARD LOCK; `run_sequence` trả thêm `debug_tag_counts` | Khối gộp trước đây **thiếu verdict**; tỉ lệ lock phải suy chặn trên vì `n` trộn 2 loại cửa sổ |
| 19 | `infer.py` + 3 config | Thêm `infer.fine_space: fused \| pre_bn` (mặc định `fused`) cho gate HARD LOCK | Để đổi không gian fine score bằng config sau khi có kết quả TAR@FAR |
| 20 | `md/15thg9.md` | Thêm §1.2 (xác nhận trên toàn dataset n=40353) | File này |
| 21 | `calibrate_threshold.py` | Thêm `rank_metrics()` (Rank-1 / mAP / mốc ngẫu nhiên, bỏ hẳn self-match) + cột trong bảng so sánh + tự kết luận "sửa luật quyết định" hay "sửa embedding" | TAR@FAR chỉ nói về ngưỡng tuyệt đối; Rank-1 mới tách được 2 nguyên nhân (§4.1 KL5) |
| 22 | `calibrate_threshold.py` | Thêm bảng **TAR tại FAR = 0.1/1/5/10%** (calibrate trên cal, đo trên eval) + lưu `far_curve` vào report | TAR@FAR=0.1% chỉ là 1 điểm làm việc; cần thấy độ dốc để biết có phải chọn sai mốc FAR (§4.1 KL6) |
| 23 | `calibrate_threshold.py` | Vector hoá `scores_from_matrix()` (thay vòng lặp Python) + tách `similarity_matrix()`; verify y hệt bản cũ trên 5 trial | Chuẩn bị cho z-norm; đồng thời nhanh hơn |
| 24 | `calibrate_threshold.py` | Thêm `per_query_znorm()` + khối thử nghiệm z-norm và lưu `znorm` vào report | Đo **trần** của hướng "đổi luật quyết định" mà không cần train lại (§4.1 KL8) |
| 25 | `calibrate_threshold.py` | Thêm khối `=== TAI NGUONG DANG TRIEN KHAI ===` (TAR/FAR offline tại đúng `infer.reid_threshold`) + lưu `deployed_threshold` | Cầu nối offline ↔ online: giải thích vì sao pipeline chỉ lock ≤6.08% (§4.1 KL7) |
| 26 | `md/15thg9.md` | Thêm §4.1 KL5–KL8 (số Rank-1/FAR-grid thật); viết lại §7, §8, §9 | File này |
| 27 | `model.py` | **SỬA LỖI**: thêm `_is_classifier_key()` + tách nhóm key classifier; cảnh báo `❌` giờ chỉ bật khi thiếu key KHÔNG phải classifier. Unit-test 9 ca | Cảnh báo `❌` ở lần chạy trước là **BÁO ĐỘNG GIẢ** — 3 key thiếu đều là đầu classifier không dùng khi eval (§6) |
| 28 | `calibrate_threshold.py` | `per_query_znorm(..., cohort=)` thêm biến thể `'impostor'` (μ/σ chỉ trên cột khác danh tính) + in bảng 2 biến thể | Biến thể `'all'` bị **thiên vị**: gallery chứa ~49 genuine/truy vấn, làm μ/σ phồng và đè z-genuine xuống (§4.1 KL8) |
| 29 | `calibrate_threshold.py` | Thêm khối `=== TAI NGUONG DANG TRIEN KHAI ===` + lưu `deployed_threshold` | Phát hiện offline TAR **38.86%** tại ngưỡng 0.75 trong khi pipeline lock **≤6.08%** → cửa sổ khó hơn ~6× (§4.1 KL7) |
| 30 | `md/15thg9.md` | Viết lại §6 (confound = báo động giả), điền số thật KL7, định lượng thiên vị KL8, đổi thứ tự ưu tiên §8 | File này |
| 31 | `calibrate_threshold.py` | Thêm không gian `visual` (2560-d) và `temporal` (512-d) vào `extract_features` + khối `=== ABLATION: nhanh TEMPORAL dong gop bao nhieu? ===` | Trả lời "temporal có ý nghĩa không?" bằng **khả năng phân biệt** (Rank-1/mAP/TAR), không bằng mức điểm (§10.1) |
| 32 | `model.py` | `load_checkpoint_verbose` thêm chẩn đoán implementation temporal: so shape `x_proj`/`dt_proj` giữa checkpoint và model → `temporal_impl_checkpoint`, `temporal_arch_ok` | Phát hiện checkpoint dùng **`SimpleS6Block` (fallback), KHÔNG phải Mamba thật** (§10.2) |
| 33 | `md/15thg9.md` | Thêm §10 trả lời 2 câu hỏi 15/9 (temporal có ý nghĩa? + 2-bank anchor/recent) | File này |
| 34 | `calibrate_threshold.py` | **SỬA LỖI**: 5 chỗ dùng sai key `'map_eval'` (đúng là `'mAP_eval'`) → crash `KeyError` ở khối ablation | Lần chạy trước dừng giữa chừng ngay sau khi in header ablation |
| 35 | `calibrate_threshold.py` | **SỬA LỖI**: khối ablation đọc `r.get('far_curve')` nhưng `far_curve` là **dict theo space** → cột `TAR@FAR1%` in `nan%` | Đã verify lại bằng số thật |
| 36 | `calibrate_threshold.py` | **SỬA LỖI**: verdict hardcode `"CA HAI khong gian"` + khẳng định `"doi LUAT QUYET DINH"` (đã bị z-norm bác bỏ) | Giờ đếm space động và nêu 2 nhánh (a)/(b) kèm phép thử |
| 37 | `md/15thg9.md` | Thêm §11 (ablation + z-norm + giới hạn suy luận + ngưỡng theo từng không gian) | File này |
| 38 | `calibrate_threshold.py` | Thêm `diagnose_top_impostors()` + khối `=== CHAN DOAN NHIEU NHAN / TRUNG DANH TINH ===` + lưu `label_noise_diagnosis` | Phép thử quyết định cho nghi phạm hàng đầu còn lại (§11.3): cặp "impostor" thực chất là cùng một vật |
| 39 | `calibrate_threshold.py` | `CalibDataset` giữ thêm `sequence_id`/`event_index` (không đổi `__getitem__` — DataLoader `shuffle=False` nên hàng feature khớp `valid_pairs`) | Cần `sequence_id` để phân biệt hard-negative cùng chuỗi với trùng danh tính khác chuỗi |
| 40 | `md/15thg9.md` | Thêm §11.7 | File này |
| 41 | `audit_identity_sequences.py` | **MỚI**: audit `identity_id ↔ sequence_id` từ JSON (không cần model/GPU) | Chẩn đoán cấp dataset: nhãn cục bộ/toàn cục, cấu trúc cặp impostor (§12.1) |
| 42 | `md/15thg9.md` | Thêm §12 (kết luận từ chẩn đoán + audit); cập nhật §7 | **340 id ↔ 340 seq song ánh, 0% impostor cùng chuỗi** → giao thức eval không khớp deployment; nhiễu nhãn chỉ ~1–5%; trội là fine-grained |
| 43 | `md/15thg9.md` | Thêm §13: vì sao `num_frames` infer phải khác lúc train | `md/test.txt` (6 run, cùng video): `visual_plain` 0.986–0.994 **miễn nhiễm** với N; chỉ `temporal` sụp **ngay tại N_train** (resnet 0.349@12, dinov3 0.579@8); N còn điều khiển số cơ hội quyết định (6/4/3) |

### 0.0. Audit `num_frames` toàn bộ config (sau khi sửa)

| Config | `train.num_frames` | `infer` | `infer_robustness` | `infer_realworld` |
|---|---|---|---|---|
| `config_colab.yaml` | 12 | 12 ✅ | 12 ✅ | — |
| `config_jetson.yaml` | 16 | 16 ✅ | 16 ✅ | 16 ✅ |
| `config_local.yaml` | 16 | 16 ✅ | — | — |
| `config_h100.yaml` | 16 | — | — | — |
| `config_jetson_smoke.yaml` | 16 | — | — | — |
| `config.json` (legacy, flat) | 16 | — | — | — |

> Lưu ý đánh đổi: cửa sổ dài hơn (16 token × stride 2 ≈ 32 frame thô) khớp train nhưng **khó thu đủ
> trong đoạn tái xuất ngắn** hơn — xem §5. Đây là lý do phải làm bước resample/pad (việc #5 ở §8),
> chứ không phải hạ `num_frames` trở lại.

### 0.1. Chạy và đọc kết quả ở đâu

```bash
# Inference (đọc ngay dòng load_checkpoint_verbose + DEBUG re-acquire)
python infer.py --config configs/config_colab.yaml \
    --seq-dir ./data/UAV-Anti-UAV/Test/UAV-Anti-UAV_Test_000059

# Phép thử quyết định
python calibrate_threshold.py --config configs/config_colab.yaml
python evaluate_reid.py --config configs/config_colab.yaml --space fused
python evaluate_reid.py --config configs/config_colab.yaml --space pre_bn
```

| Kết quả nằm ở | Nội dung |
|---|---|
| `infer_output/<seq>/metrics.txt` | Toàn bộ log stdout của sequence, gồm các dòng `DEBUG re-acquire` và khối `PERFORMANCE METRICS` |
| `infer_output/<out_dir>/summary_metrics.txt` | Bản gộp khi `infer.seq_dir: all`, có thêm `Sim PRE-BN` / `Sim POST-BN` / `BN degradation` trên toàn bộ sequence |
| `<eval.output_dir>/evaluation_report.json` | Khoá phẳng = không gian chính; `spaces` = bảng đầy đủ 2 không gian |
| `<calib.output_dir>/calibrated_threshold.json` | `threshold` (chính) + `thresholds: {fused, pre_bn}` |
| `<calib.output_dir>/calibration_report.json` | `spaces` = TAR/FAR cal + eval của từng không gian |

### 0.2. Lần chạy đã dùng để lấy số liệu dưới đây

Sequence `UAV-Anti-UAV_Test_000059`, checkpoint `/content/best_model.pth`, backbone `dinov3_convnext`,
`infer.num_frames = 12`, `soft_lock_threshold = 0.3`, `reid_threshold = 0.75`.

---

## 1. Số liệu thực đo

**18 lần in debug = 17 re-acquire + 1 anti-hijack** (frame 663).

| Tầng | min | mean | max |
|---|---|---|---|
| `visual_shot` (weighted, coarse) | 0.985 | 0.989 | 0.995 |
| `visual_plain` (input head) | 0.987 | **0.990** | 0.997 |
| `temporal` (đầu ra Mamba) | 0.910 | **0.938** | 0.970 |
| **PRE-BN `raw`** = `cat(visual_plain, temporal)` | 0.931 | **0.953** | 0.978 |
| **POST-BN `fused`** = `bnneck(...)` | 0.663 | **0.734** | 0.896 |

Kiểm tra tính nhất quán: `raw` (0.953) đúng bằng trung bình có trọng số giữa `visual_plain`
(0.990) và `temporal` (0.938) — nghĩa là phép ghép hoạt động đúng như lý thuyết, không có
"điều bí ẩn" nào ở đầu vào của head. Toàn bộ phần mất mát nằm ở **bnneck**: `0.953 → 0.734`.

Với ngưỡng `reid_threshold = 0.75`:

| Không gian fine score | Vượt ngưỡng |
|---|---|
| POST-BN `fused`, chỉ 17 dòng re-acquire | **3/17** (frame 140 = 0.750, 551 = 0.759, 682 = 0.896) |
| POST-BN `fused`, tính cả anti-hijack | 4/18 |
| PRE-BN `raw` | **18/18** |

**Hệ quả cụ thể** (đối chiếu bảng 8 ca ở §5): có **4 ca tái xuất thu đủ cửa sổ** và tạo được
fine score. Nếu lấy `raw` làm fine score, **cả 4/4 ca này HARD LOCK ngay cửa sổ đầu tiên**:

| Ca (reappear → cửa sổ đầu) | Thực tế với `fused` | Nếu dùng `raw` |
|---|---|---|
| 122 → 134 | lock ở 140 (latency 18) | lock ở 134 (latency 12) |
| 214 → 226 | **không bao giờ lock** (0.708, 0.712 rồi mất dấu) | lock ở 226 (latency 12) |
| 533 → 545 | lock ở 551 (latency 18) | lock ở 545 (latency 12) |
| 670 → 682 | lock ở 682 (latency 12) | lock ở 682 (giữ nguyên) |

4 ca còn lại **không đổi** vì cửa sổ không bao giờ đầy (§5).

---

## 1.2. Xác nhận trên TOÀN BỘ dataset (`infer.seq_dir: all`, n=40353) — 15/9

Lần chạy gộp mọi sequence có sự kiện mất dấu (config_colab, `num_frames=12`, `reid_threshold=0.75`):

```
Avg Re-acquisition Latency : 26.77 frames
Total False Alarms         : 37900
Sim PRE-BN  (raw concat)   : 0.916 (n=40353)
Sim POST-BN (fused/fine)   : 0.612
BN degradation (pre - post) : +0.305
```

| Chỉ số | 1 sequence (`000059`, n=18) | Toàn bộ (n=40353) |
|---|---|---|
| `raw` (pre-BN) | 0.953 | **0.916** |
| `fused` (post-BN) | 0.734 | **0.612** |
| delta (BN nén) | +0.218 | **+0.305** |
| Latency TB | 16.00 frame | 26.77 frame |

**KL1 — BN nén cosine là hiện tượng CÓ HỆ THỐNG.** Delta trên toàn dataset (0.305) còn **lớn hơn**
trên một sequence (0.218), nên đây không phải đặc thù `000059`. Củng cố §2.3.

**KL2 — con số đáng lo nhất: ít nhất 93.92% cửa sổ fail ngưỡng.**
`false_alarms` chỉ tăng ở nhánh re-acquire (`infer.py:428`), còn `n=40353` là tổng của re-acquire
**và** anti-hijack, nên suy được **chặn trên chính xác**:

| Suy luận | Giá trị |
|---|---|
| `n_re-acquire ≥ n_false_alarms` | ≥ 37900 → `n_anti-hijack ≤ 2453` |
| HARD LOCK = `n_re-acquire − 37900` | **≤ 2453** |
| Tỉ lệ lock | **≤ 6.08%** |
| Tỉ lệ re-acquire fail | **≥ 93.92%** |

**KL3 — khớp với `9thg9.md`, và chỉ ra nút thắt KHÔNG phải "chọn ngưỡng".**
Ngưỡng calibrate cho không gian post-BN tại FAR=0.1% là **0.852** — tức **cao hơn** ngưỡng 0.75 mà
pipeline đang dùng. Nghĩa là pipeline đã dùng ngưỡng *dễ hơn* ngưỡng calibrated, mà vẫn fail ~94%.
Hai phép đo độc lập gặp nhau ở cùng một chỗ:

| Đo | Giá trị | Ngưỡng |
|---|---|---|
| Offline TAR@FAR=0.1%, không gian `fused` (`9thg9.md`) | **8.45%** | 0.852 |
| Online lock rate, không gian `fused` (lần chạy này) | **≤ 6.08%** | 0.75 |

⇒ Không gian `fused` chỉ có TAR một chữ số. Hạ hay nâng ngưỡng cũng không cứu được; câu hỏi thật
là **không gian `pre_bn` có TAR@FAR tốt hơn không** (§4) — và đó là phép thử còn thiếu duy nhất.

**⚠️ Cảnh báo diễn giải.** `raw = 0.916` cao *có thể* là vì mọi thứ đều giống nhau (mất khả năng
phân biệt), chứ không phải vì khớp tốt. Con số n=40353 **không phải TAR** — các cửa sổ này không có
nhãn genuine/impostor. Chỉ `calibrate_threshold.py` + `evaluate_reid.py --space pre_bn` trả lời được.

**Đã bổ sung để lần chạy sau tự trả lời:**

| Bổ sung | Vì sao |
|---|---|
| `format_bn_debug_lines()` dùng chung cho cả khối per-sequence **và** `=== AGGREGATED METRICS ===` | Dòng `-> verdict` **không xuất hiện** trong kết quả của bạn: verdict trước đây chỉ được thêm ở khối per-sequence, khối gộp thì không |
| Thêm dòng tách tag + tỉ lệ lock | `re-acquire N \| anti-hijack M -> HARD LOCK x/N`, để có tỉ lệ chính xác thay vì phải suy chặn trên |
| Thêm cờ `infer.fine_space: fused \| pre_bn` (mặc định `fused`) | Đổi không gian gate HARD LOCK bằng config, không cần sửa code. Gate anti-hijack vẫn giữ trên `fused` + `hijack_threshold` riêng; cột debug POST-BN luôn ghi `fused` thật nên bảng so sánh không bị lẫn |

Output khối gộp sẽ có dạng:

```
Sim PRE-BN  (raw concat)   : 0.916 (n=40353)
Sim POST-BN (fused/fine)   : 0.612
BN degradation (pre - post) : +0.305 -> BN nén cosine xuống dưới ngưỡng 0.75 -> BN là mắt xích
                              đang chặn HARD LOCK. LƯU Ý: raw cao hơn KHÔNG chứng minh phân biệt
                              tốt hơn (impostor cũng cao hơn); cần TAR@FAR: ...
Cua so vuot nguong 0.75    : raw X%  |  fused Y%   (toan bo 40353 cua so; KHONG phai FAR)
Phan bo theo tag           : re-acquire N | anti-hijack M   -> HARD LOCK x/N = Z% (chi tinh cua so re-acquire)
```

---

## 2. BA KẾT LUẬN SAI của `14thg9.md`

### ❌ 2.1. "temporal (Mamba) 0.47–0.74, dao động, là phần kém ổn định"

Số thực đo: **0.910 – 0.970, mean 0.938** — gần bằng visual (0.990), độ tán xạ rất nhỏ.

Nhiều khả năng nguyên nhân của con số cũ là **lệch số token train/infer**: `train.num_frames = 12`
nhưng `infer.num_frames = 8` (trước khi sửa). Mamba + mean-pooling trên 8 token ≠ trên 12 token
→ temporal token lệch phân phối ngay từ gốc. Sau khi khớp số token, temporal gần như bám sát visual.

> Caveat: log cũ trong `14thg9.md` có thể được chạy với checkpoint/sequence khác, nên không
> khẳng định 100% rằng `num_frames` là nguyên nhân duy nhất. Nhưng mức nhảy 0.5 → 0.94 rất khó
> giải thích bằng bất kỳ thay đổi nào khác.

### ❌ 2.2. "fused 0.04–0.51 → BN khuếch đại khác biệt nhỏ thành khổng lồ"

Con số `0.04` **không tái hiện được**: lần này `fused` = 0.663 – 0.896. Giá trị thấp nhất là
`0.663` (frame 134) — vẫn cao hơn hẳn 0.04.

Con số `0.04` cũ nhiều khả năng là **hệ quả của lệch số token** (temporal 0.52 khi đó, xem log
`[200] DEBUG sim: ... temporal=0.520 ... fused=0.054`), chứ không phải bằng chứng của việc BN
"bẻ cong không gian đặc trưng".

### ❌ 2.3. "Cơ chế: BN khuếch đại các chiều có variance nhỏ (`γ_i/σ_i` lớn)"

Cơ chế này dự đoán delta `raw − fused` sẽ **thất thường, phụ thuộc từng cặp** (cặp mất 0.9,
cặp mất 0.02). Số thực đo thì ngược lại:

| Đo | Giá trị |
|---|---|
| `delta = raw − fused` | mean **0.218**, đa số 0.20 – 0.27, ngoại lệ 0.082 và 0.137 |
| Tương quan `delta` với `raw` | **r = −0.77** (raw càng cao → delta càng nhỏ) |
| Hồi quy | `delta ≈ 3.105 − 3.031 × raw` (raw 0.93 → 0.29 ; raw 0.98 → 0.14) |

Delta ổn định và tỉ lệ nghịch với độ tương đồng là dấu hiệu của **một phép biến đổi tuyến tính
nén cosine một cách hệ thống (recalibration thang điểm)**, không phải "khuếch đại ngẫu nhiên
theo từng chiều". BN neck là phép affine cố định ở eval mode — nó có thể nén cosine, nhưng nén
đều chứ không phá hủy riêng lẻ.

> Ghi chú: đoạn văn "không có hệ số khuếch đại riêng cho từng chiều" của `14thg9.md` khi nói về
> LayerNorm cũng chưa chuẩn — LayerNorm vẫn có `γ_i, β_i` học được; cái nó loại bỏ là `1/σ_i`.

---

## 3. KẾT LUẬN ĐÚNG của `14thg9.md`

**BN neck là mắt xích trực tiếp chặn HARD LOCK.** Điều này giờ đã đo được chứ không còn là
suy luận: input head rất giống nhau (`raw` 0.953) nhưng output head tụt còn 0.734, và ngưỡng
0.75 nằm đúng trong khoảng bị mất đó. Với 4 ca tái xuất thu đủ cửa sổ, dùng `raw` làm fine
score là đủ để lock ngay cửa sổ đầu (§1).

> ✅ **Đúng về mặt cơ chế, nhưng đã trở nên VÔ NGHĨA sau phép thử ở §4.1**: đổi sang `raw` làm
> TAR giảm 2.67 điểm. "Chặn HARD LOCK" chỉ là hệ quả của việc ngưỡng 0.75 không thuộc không gian
> `pre_bn` — không phải bằng chứng BN gây hại.

---

## 4. NHƯNG: "chặn HARD LOCK" ≠ "là lỗi"

Đây là điểm logic quan trọng nhất mà `14thg9.md` bỏ qua:

> **Điểm cao hơn không tự động là tốt hơn.** Ở không gian `raw`, impostor cũng sẽ có điểm cao
> hơn. Việc `raw` vượt ngưỡng 0.75 chỉ chứng minh rằng **ngưỡng 0.75 được calibrate cho không
> gian `fused`** — không chứng minh BN làm mất khả năng phân biệt.
>
> ✅ **Đã kiểm chứng xong ở §4.1**: dự đoán này ĐÚNG. `pre_bn` kém hơn 2.67% TAR.

Ngưỡng `0.852` trong `9thg9.md` (TAR@FAR=0.1% → 8.45%) được tính trên embedding **post-BN**.
Muốn biết BN có hại thật hay không, phải so **separability** ở cả 2 không gian bằng cùng một
quy trình: Rank-1/mAP và **TAR@FAR=0.1%** với threshold calibrate trên cal-split.

→ Đã bổ sung chính xác phép thử này (#9–#12 ở §0):

| File | Thay đổi |
|---|---|
| `evaluate_reid.py` | Trích đồng thời `fused` + `pre_bn` trong 1 lượt backbone; in bảng so sánh + `ΔTAR`; thêm `--space fused\|pre_bn`; `evaluation_report.json` có thêm khoá `spaces` |
| `calibrate_threshold.py` | Calibrate **cả 2 không gian** với cùng quy trình Fixed-FAR; `calibrated_threshold.json` có thêm `thresholds: {fused, pre_bn}`; in bảng `ΔTAR(eval)` kèm kết luận tự động |

Cách đọc `ΔTAR(eval, pre_bn − fused)`:

| Kết quả | Kết luận | Hành động |
|---|---|---|
| `> +2%` | BN thật sự làm mất khả năng phân biệt | Đáng đổi `ReIDHead` (LayerNorm/bỏ BN) + train lại Stage 1 |
| `±2%` | BN vô hại, chỉ nén thang điểm | **Không cần train lại** — chỉ calibrate lại threshold cho không gian `fused` |
| `< −2%` | BN có ích | Giữ nguyên `ReIDHead` |

> ⚠️ Bảng trên chỉ xét **chênh lệch tương đối**. Nếu **cả hai** không gian đều TAR thấp thì kết luận
> đúng là "nút thắt không phải BN" — và đó chính xác là trường hợp đã xảy ra, xem §4.1.

---

## 4.1. KẾT QUẢ PHÉP THỬ QUYẾT ĐỊNH (15/9) — BN KHÔNG phải thủ phạm

`python calibrate_threshold.py --config configs/config_colab.yaml`
Protocol: Fixed FAR ≤ 0.1%. Cal = 136 sequences (931 pairs), eval = 204 sequences (1699 pairs).
`genuine = 19,790` / `931×930` = **2.29%** (chú ý: `compute_scores` bỏ self-match).

| Space | t* | TAR@t\* (cal) | TAR@t\* (eval) | FAR@t\* (eval) |
|---|---|---|---|---|
| `fused` (post-BN) | 0.852396 | 14.28% | **8.45%** | 0.1096% |
| `pre_bn` (raw) | **0.971263** | 10.75% | **5.79%** | 0.1081% |

`ΔTAR(eval, pre_bn − fused) = −2.67%` → **`pre_bn` KÉM HƠN.**

### KL1 — Giả thuyết trung tâm của `14thg9.md` bị BÁC BỎ

Bỏ BN **không những không giúp mà còn làm TAR giảm 2.67 điểm**. Hệ quả trực tiếp:

- ❌ KHÔNG đổi `ReIDHead` sang LayerNorm/bỏ BN, **KHÔNG train lại Stage 1** vì lý do này.
- ✅ Giữ `infer.fine_space: fused` (giá trị mặc định đã đặt trong 3 config).

### KL2 — Vì sao `pre_bn` kém hơn là **hợp lý**: lỗi phương pháp từ đầu

`HardTripletLoss` được áp trên feature **SAU BN** đã L2-normalize (`train_reid.py:468-471`).
Nghĩa là **không gian post-BN CHÍNH LÀ không gian mà loss đã tối ưu**; `pre_bn` chỉ là biểu diễn
trung gian **chưa từng được huấn luyện trực tiếp**. Đem chấm điểm ở không gian chưa tối ưu rồi
kết luận "BN phá cosine" là **sai ngay từ giả thiết**.

Hệ quả: con số `0.953 → 0.734` ở §1 **không phải "mất mát"** mà là head đang làm việc — nén cosine
xuống và tách các lớp ra. Đó là lý do `fused` có t* thấp hơn (0.852 < 0.971): phân phối trải rộng hơn.

### KL3 — Nhưng CẢ HAI không gian đều HỎNG, và đây mới là nút thắt

TAR 8.45% tại FAR 0.1% nghĩa là **chỉ bắt được <9% số lần tái xuất thật**. Và:

- t\* của `pre_bn` = **0.9713** → 99.9% cặp impostor đều nằm **dưới 0.971**, tức cosine bị dồn vào
  khoảng `[0.93, 1.0]` cho **cả** genuine lẫn impostor.
- Đây đúng là dấu hiệu **embedding thoái hoá (collapse)** — mọi thứ đều "giống nhau".
- Khớp chính xác với cảnh báo ở §1.2: `raw = 0.916` cao **không phải vì khớp tốt**.

### KL4 — cal → eval sụt mạnh

`fused` 14.28% → **8.45%** (giảm 1.7×); `pre_bn` 10.75% → 5.79%. Ngưỡng calibrate trên cal-split
bị **lạc quan rõ rệt** → mọi con số công bố phải là số **eval-split**.

### KL5 — Rank-1: chỉ số quyết định (đã đo)

TAR@FAR chỉ nói về **ngưỡng tuyệt đối**, không nói embedding có xếp hạng đúng hay không.
Kết quả đo trên eval-split (204 sequences, 1699 pairs):

| Space | Rank-1 (eval) | mAP | Mốc ngẫu nhiên |
|---|---|---|---|
| `fused` | **78.15%** | 35.59% | 2.90% |
| `pre_bn` | 67.32% | 27.81% | 2.90% |

> Mốc ngẫu nhiên = `83,708 genuine / (1699×1698)` = **2.90%** (đã bỏ self-match).

**Rank-1 = 78.15% so với mốc ngẫu nhiên 2.90% → xếp hạng TỐT.** Embedding **có** phân biệt được
danh tính; nó không hề bị "collapse" như tôi nghi ở KL3. Vậy mâu thuẫn "Rank-1 78% mà
TAR@FAR=0.1% chỉ 8.45%" giải thích thế nào?

| Chỉ số | Yêu cầu | Kết quả |
|---|---|---|
| Rank-1 | cặp genuine phải **thắng các cặp khác TRONG CÙNG truy vấn** | 78.15% ✅ |
| TAR@FAR=0.1% | một **ngưỡng cosine TUYỆT ĐỐI dùng chung cho mọi truy vấn** phải tách được | 8.45% ❌ |

Nghĩa là: **thứ tự trong mỗi truy vấn đúng, nhưng MỨC điểm không so sánh được giữa các truy vấn.**
Mỗi truy vấn có offset/scale riêng; chỉ một nhóm nhỏ truy vấn có impostor điểm rất cao kéo
phân vị 99.9% toàn cục lên 0.852, và ngưỡng đó vô hiệu hoá phần lớn truy vấn còn lại.

**mAP 35.59% ≪ Rank-1 78.15%** là chi tiết quan trọng: top-1 thường đúng, nhưng **các cặp genuine
khác bị xếp dưới impostor** — tức độ tương đồng *trong cùng danh tính* dao động rất mạnh. Đây đúng
là vấn đề của pipeline: bank neo (trước khi mất dấu) và cửa sổ tái xuất có thể rất khác nhau.

### KL6 — Điểm làm việc FAR: số THẬT (thay ước lượng Gaussian)

TAR (eval-split) theo mốc FAR, threshold calibrate trên cal-split (out-of-sample):

| Space | FAR 0.1% | FAR 1% | FAR 5% | FAR 10% |
|---|---|---|---|---|
| `fused` | 8.5% | **23.2%** | **41.4%** | **51.9%** |
| `pre_bn` | 5.8% | 15.9% | 31.0% | 41.4% |

⇒ Hai điều cùng đúng, phải tách bạch:

1. **Điểm làm việc quan trọng**: FAR 0.1% → 1% làm TAR tăng ~2.7×. Mốc FAR 0.1% là chuẩn open-set
   ReID, **không nhất thiết** đúng cho gate re-acquisition (crop đã được lọc bởi `soft_lock_threshold`
   và bởi bbox GT — "impostor" ở đây không cùng phân phối với impostor của pipeline).
2. **Nhưng không phải chỉ có thế**: ngay ở FAR 10% (rất dễ dãi) TAR vẫn chỉ ~52%. Nên ngoài luật
   quyết định, **chất lượng cặp genuine cũng còn yếu** — khớp với mAP chỉ 35.59%.

### KL7 — Cầu nối offline ↔ online (đã thêm phép đo)

Pipeline dùng `reid_threshold` và chỉ HARD LOCK ≤ 6.08% số cửa sổ (§1.2). Offline tại **đúng ngưỡng
đó** (0.75) thì TAR là bao nhiêu? ✅ **Đã đo:**

| Space | Ngưỡng | TAR (eval) | FAR (eval) |
|---|---|---|---|
| `fused` | 0.75 (đang triển khai) | **38.86%** | **6.1393%** |
| `pre_bn` | 0.75 | 100.00% | 99.9979% |

Ba kết luận:

1. ⭐ **Offline TAR = 38.86% > > online lock ≤ 6.08% → cửa sổ T2_SEARCH KHÓ HƠN dữ liệu
   query/gallery khoảng 6×.** Đây là phát hiện quan trọng nhất của lần chạy này, và nó **chuyển
   trọng tâm khỏi ngưỡng**: ngay cả khi calibrate hoàn hảo, ngưỡng không thể bù được việc cửa sổ
   có điểm thấp. Nút thắt nằm ở chất lượng **cửa sổ / bank tham chiếu**, không phải ở threshold.
2. **Ngưỡng 0.75 đang chạy ở FAR = 6.14%**, tức **56× mức mục tiêu 0.1%**. Xác nhận lại mâu thuẫn ở
   §9: `reid_threshold=0.75` (tay đặt) và `t*=0.8524` (calibrate cho FAR 0.1%) không thể cùng đúng.
3. `pre_bn` cho TAR=100% với FAR=99.9979% — điểm `raw`/`pre_bn` gần như **luôn ≥ 0.75**, nên ngưỡng
   0.75 chấp nhận **mọi** cặp. Đây là minh chứng định lượng cho việc "raw vượt 0.75 ở cả 18/18
   event" (§2) **không có nghĩa gì** — nó chỉ nói ngưỡng 0.75 thuộc thang đo của `fused`.

**Giả thuyết cho khoảng cách 6×** (chưa kiểm chứng hết):

| # | Giả thuyết | Ghi chú |
|---|---|---|
| a | Bank tham chiếu **quá nhỏ / quá ít đa dạng**: offline lấy max trên **~49** khung `before` của cùng danh tính (nhiều event, nhiều góc); pipeline lấy max trên **≤5 anchor + ≤15 recent** | `infer.py:161` `_max_sim` duyệt `anchor_bank + recent_bank`; `max_anchor_size: 5`. Hơn nữa `add_anchor` **đóng băng 5 khung ĐẦU TIÊN** (`if len(...) < self.max_anchor`, `infer.py:145`) — có thể là 5 khung chất lượng kém nhất |
| b | Cửa sổ tái xuất chứa khung **che khuất một phần** (target đang hiện dần) | 4/8 event không đủ 12 khung (§5) |
| c | Cửa sổ 12 khung trải qua **pha chuyển tiếp** → temporal token khác phân phối với clip `after` sạch của offline | |

→ Hai giả thuyết (a) và (b) đều **sửa được mà không cần train lại**. Ưu tiên (a) vì rẻ nhất.

### KL8 — Thử nghiệm "đổi luật quyết định": z-norm theo từng truy vấn (đã thêm)

Để đo **trần** của hướng sửa rẻ nhất (không train lại), `calibrate_threshold.py` tính thêm
`per_query_znorm`: chuẩn hoá điểm theo từng truy vấn `z = (s − μ_i) / σ_i` rồi calibrate lại
threshold. Vì là biến đổi **affine theo hàng** nên **Rank-1 không đổi** — chỉ DET/TAR đổi.

**Kết quả lần chạy 15/9 (biến thể `cohort='all'`):**

| Space | t\* (z) | TAR@FAR=0.1% | so với gốc |
|---|---|---|---|
| `fused` | 3.0753 | 6.13% | **−2.32%** |
| `pre_bn` | 2.4718 | 4.78% | **−1.01%** |

⚠️ **Nhưng biến thể này THIÊN VỊ CHỐNG z-norm**: nó lấy μ/σ trên **toàn bộ gallery**, mà gallery có
**~49 cặp genuine mỗi truy vấn** (2.9%) — và chúng chính là **các điểm CAO NHẤT**. Đưa chúng vào μ/σ
làm μ, σ phồng lên và **đè z-score của chính genuine xuống**. Đó là lý do z-norm "thất bại".

→ **Đã thêm biến thể thứ hai `per_query_znorm(..., cohort='impostor')`**: μ/σ chỉ tính trên các cột
**khác danh tính**. Đây mới là **upper bound đúng** của hướng chuẩn hoá điểm (t-norm kinh điển).
Lưu ý nó **dùng nhãn** để chọn cohort → deployment thật phải thay bằng một **cohort tham chiếu có
nhãn** cố định, không phải toàn gallery.

⇒ **Trạng thái: chưa kết luận được** hướng "đổi luật quyết định" là đúng hay sai — phải chạy lại
`calibrate_threshold.py` và đọc **cả hai** dòng `cohort=all` / `cohort=impostor`. Nếu
`impostor` ≫ `all` thì bản `all` đúng là thiên vị và hướng này còn sống; nếu cả hai đều ≤ gốc thì
**luật quyết định bị loại** và nút thắt nằm ở KL7 (cửa sổ/bank).

## 5. PHÁT HIỆN MỚI #1: nút thắt cửa sổ 12 frame (bị `14thg9.md` bỏ sót)

8 lần UAV tái xuất trong sequence `000059`:

| Loại | Số lần | Frame |
|---|---|---|
| HARD LOCK | 3 | 140, 551, 682 |
| Hoàn thành cửa sổ nhưng fine fail | 1 | 226–227 (0.708, 0.712) |
| **Bỏ dở vì mất dấu giữa chừng** | **4** | 109 (8/12), 236 (6/12), 244 (6/12), 261 (10/12) |

**Một nửa số lần tái xuất chưa bao giờ tạo được fine score** — sửa BN cũng không cứu được 4 ca này
(không có cửa sổ nào để chấm điểm). Nguyên nhân thiết kế: lúc train, 12 frame được sample bằng
`linspace` trên cả clip; lúc inference, pipeline lại **đòi thu đủ 12 frame có mặt liên tục**.
Với các đoạn tái xuất ngắn (6–10 frame), cửa sổ không bao giờ đầy.

Hướng sửa (chưa làm): **resample cửa sổ có sẵn lên đúng 12 token** (nội suy / lặp feature cuối,
giống logic pad đã có ở nhánh `T0_INIT`/`T3_VERIFIED` trong `infer.py`) thay vì vứt bỏ.

Ngoài ra: `False Alarms (Fine Fails) = 14` — fine score nằm sát ngưỡng (0.70–0.76) rồi mới vượt,
đúng với việc `fused` bị nén xuống quanh 0.73.

---

## 6. PHÁT HIỆN MỚI #2: confound khi load checkpoint — ✅ ĐÃ GIẢI QUYẾT (BÁO ĐỘNG GIẢ)

### 6.1. Nghi vấn ban đầu

Log chạy có dòng:

```
Cảnh báo: Không tìm thấy pre-trained weights tại /content/UAV/gasnet_project/test/gasnet.best.pth
```

Nghĩa là trọng số VRU của GASNet **không được nạp**, nên tôi lo visual backbone chỉ có pretrain
DINOv3 chứ không phải trọng số đã train → mọi kết luận sẽ nhiễu.

### 6.2. Bằng chứng từ `load_checkpoint_verbose` (lần chạy 15/9)

```
[calibrate] best_model.pth: 889/892 keys của model được nạp (892 keys trong file)
[calibrate] ⚠️ MISSING 3 keys:   backbone.* : 2 keys | head.* : 1 key
[calibrate] ⚠️ SHAPE MISMATCH 3 keys:
    - backbone.classifier_global.weight: checkpoint(502, 768) vs model(1000, 768)
    - backbone.classifier_fs.weight:     checkpoint(502, 192) vs model(1000, 192)
    - head.classifier.weight:            checkpoint(502, 1472) vs model(1000, 1472)
```

**889/892 keys = 99.66% được nạp.** 3 key còn lại **đều là đầu CLASSIFIER**, và cả 3 đều
**không nằm trên đường trích feature**:

| Key | Vai trò | Có dùng khi eval? |
|---|---|---|
| `backbone.classifier_global` | đầu phân loại pretrain của DINOv3/GASNet | ❌ không |
| `backbone.classifier_fs` | đầu phân loại pretrain (feature-space) | ❌ không |
| `head.classifier` | đầu ID classifier trong `ReIDHead` | ❌ không — xem dưới |

Chứng cứ trực tiếp trong `model.py`:

```python
class ReIDHead(nn.Module):
    def forward(self, visual_feat, temporal_token):
        feat    = torch.cat([visual_feat, temporal_token], dim=-1)
        bn_feat = self.bnneck(feat)
        if not self.training:
            return bn_feat          # ← EVAL trả về TRƯỚC khi gọi self.classifier
        logits = self.classifier(bn_feat)
        return feat, bn_feat, logits
```

`head.classifier` **chỉ tồn tại để phục vụ classification loss khi training**. Khi eval,
`forward` trả `bn_feat` và không bao giờ chạm tới nó.

Lệch shape `502` vs `1000` cũng là chuyện **bình thường**: checkpoint train với **502 danh tính**,
còn model được khởi tạo `num_identities=1000` (giá trị mặc định) — không liên quan gì tới chất
lượng đặc trưng.

### 6.3. Kết luận — và một lỗi trong công cụ của tôi

✅ **KHÔNG có confound.** Visual backbone + temporal + bnneck **được nạp đầy đủ** từ
`best_model.pth`; các kết quả eval/infer ở trên là của **model đã train**, không phải pretrain.
Confound ở §6 bản trước **bị loại** và không còn là nghi phạm.

❌ **Nhưng cảnh báo `❌ CẢNH BÁO NGHIÊM TRỌNG` mà tôi thêm ở bản trước là BÁO ĐỘNG GIẢ.** Logic cũ
chỉ kiểm tra `k.startswith('backbone.')` mà không phân biệt **đầu classifier** với **trọng số
trích feature**, nên đã kết luận sai rằng "visual backbone đang chạy bằng init/pretrain".

→ **Đã sửa** (`model.py`): thêm `_is_classifier_key()`, tách riêng nhóm key classifier; cảnh báo
nghiêm trọng **chỉ bật khi thiếu key KHÔNG phải classifier** của `backbone.*` hoặc `head.*`
(loại trừ `bnneck`). Khi chỉ thiếu classifier, in dòng `ℹ️` giải thích kèm số lớp
`checkpoint=502 vs model=1000`. Đã unit-test 9 ca (gồm `backbone.model.1.weight` phải **vẫn**
kích hoạt cảnh báo, `head.bnneck.*` không được coi là classifier).

> 📌 Nếu muốn eval "sạch" với đúng `num_identities` của checkpoint: truyền `num_identities=502`
> khi khởi tạo model. **Không cần thiết** cho matching vì classifier không dùng khi eval.

---

## 7. Trả lời trực tiếp câu hỏi "gốc rễ có phải phần temporal (Mamba)?"

**KHÔNG.** Dữ liệu phủ định giả thuyết đó: temporal = 0.910–0.970, gần bằng visual 0.990.
Và sau phép thử ở §4.1: **cũng KHÔNG phải BatchNorm1d**. Bức tranh đúng hiện tại:

| # | Nghi phạm | Trạng thái | Bằng chứng |
|---|---|---|---|
| 1 | Mamba / temporal | ❌ **Đã loại** | temporal 0.938 ≈ visual 0.990; lỗi cũ là do lệch số token (đã sửa) |
| 2 | BatchNorm1d (`bnneck`) | ❌ **Đã loại** | `pre_bn` KÉM hơn `fused` 2.67% TAR (§4.1); loss lại được áp SAU BN nên post-BN mới là không gian đã tối ưu |
| 3 | Embedding không phân biệt được | ❌ **Đã loại** | Rank-1 = **78.15%** vs mốc ngẫu nhiên 2.90% → embedding phân biệt tốt (§4.1 KL5) |
| 4 | Luật quyết định (ngưỡng cosine tuyệt đối) | ❌ **ĐÃ LOẠI** | z-norm theo từng truy vấn (cả `cohort=all` và `cohort=impostor`) chỉ cải thiện tốt nhất **+0.22%** ⇒ điểm cosine ĐÃ tương thích giữa các truy vấn (§11.2) |
| 5 | Confound nạp checkpoint | ❌ **ĐÃ LOẠI** | 889/892 keys được nạp; 3 key thiếu đều là **đầu classifier không dùng khi eval** (§6) |
| 6 | Cửa sổ T2_SEARCH / bank tham chiếu quá yếu | ⚠️ **GIẢ THUYẾT, CHƯA KIỂM CHỨNG** | Offline tại cùng ngưỡng 0.75: TAR **38.86%**; pipeline chỉ lock **≤6.08%** → cửa sổ khó hơn ~6× (§4.1 KL7) |
| 8 | Nhiễu nhãn / trùng danh tính | ❌ **ĐÃ LOẠI (chỉ là phần nhỏ)** | Có thật (719↔720 bạn xác nhận cùng 1 cái; 7/10 cặp dễ nhầm có \|Δpid\|≤3 ≈ làm giàu 39×) nhưng chỉ ~1–5% khối lượng; 493 cặp pid, top share 6.3% → PHÂN TÁN (§12.2) |
| 9 | **Giao thức eval không khớp deployment** | ⭐⭐ **ĐÃ XÁC NHẬN** | 340 id ↔ 340 seq (song ánh); impostor CÙNG chuỗi = **0%**, KHÁC chuỗi = **100%** → TAR@FAR đo bài toán khác (§12.1) |
| 10 | **Fine-grained: drone cùng loại** | ⭐⭐ **XÁC NHẬN (một phần)** | 63↔108 (0.9366) và 108↔237 (0.9327) bạn xác nhận **2 UAV KHÁC NHAU** mà vẫn vượt ngưỡng; mAP 35.59% ≪ Rank-1 78.15% (§12.3) |
| 7 | Cửa sổ `num_frames` khó thu đủ | ✅ Có thật, độc lập | 4/8 ca ở §5 không bao giờ có cửa sổ; sửa BN cũng không cứu |

**Thứ tự ưu tiên hành động đã đổi hoàn toàn so với `14thg9.md`**: việc cần làm không phải là
sửa `ReIDHead` và cũng **không** phải train lại.

Sau lần chạy 15/9, nghi phạm hàng đầu **dịch chuyển** từ "luật quyết định" sang
**"cửa sổ T2_SEARCH / bank tham chiếu quá yếu"**: offline tại cùng ngưỡng 0.75 cho TAR **38.86%**
trong khi pipeline lock **≤6.08%** — khoảng cách ~6× mà ngưỡng không thể bù. Hướng "đổi luật quyết
định" (KL8) **chưa kết luận** vì biến thể đo được (`cohort='all'`) bị thiên vị; phải chạy
`cohort='impostor'` mới biết.

### 7.1. Về câu kết luận tự động in ra ở cuối `infer.py`

Lần chạy này in:

```
Sim PRE-BN  (raw concat)   : 0.953 (n=18)
Sim POST-BN (fused/fine)   : 0.734
BN degradation (pre - post) : +0.218 -> Chưa kết luận rõ (raw và fused chênh không nhiều)
```

Câu **"chênh không nhiều" là SAI/cụt lủn** — chênh tới 0.218 và nó là toàn bộ khoảng cách tới
ngưỡng. Nguyên nhân: heuristic cũ chỉ có 2 nhánh (`fused < 0.60` mới gọi là "BN là thủ phạm"),
trong khi trường hợp thực tế là `raw 0.953 ≥ 0.75 > fused 0.734`, rơi vào nhánh mặc định.

→ Đã sửa lại logic verdict trong `infer.py` (so với `reid_threshold` thay vì các mốc cứng
0.60/0.70), đồng thời nhắc rõ **điểm raw cao hơn không chứng minh phân biệt tốt hơn**, phải
chạy `calibrate_threshold.py` + `evaluate_reid.py --space pre_bn`. Lần chạy sau sẽ in:

```
BN degradation (pre - post) : +0.218 -> BN nén cosine xuống dưới ngưỡng 0.75 -> BN là mắt xích
đang chặn HARD LOCK. LƯU Ý: raw cao hơn KHÔNG chứng minh phân biệt tốt hơn (impostor cũng cao
hơn); cần TAR@FAR: calibrate_threshold.py rồi evaluate_reid.py --space pre_bn
```

---

## 8. Việc cần làm tiếp (theo thứ tự)

> 🔄 **ĐÃ ĐỔI MỤC TIÊU (15/9 lần 4)**: `TAR@FAR` xuyên chuỗi KHÔNG phải metric của bài toán pipeline
> (§12.1). Ưu tiên mới nằm ở việc dựng phép đo đúng, không phải ở việc tối ưu con số cũ.

0. ⭐⭐⭐ **Dựng validation khớp deployment (TRONG CÙNG chuỗi)** — dùng `disappearance_duration_frames`
   + `event_index` đã có trong `query_test.json`:
   - **Dương**: cửa sổ sau khi tái xuất → **PHẢI** lock
   - **Âm**: cửa sổ trong khoảng target **VẮNG MẶT** → **KHÔNG ĐƯỢC** lock
   Protocol hiện tại **không có tập âm này** (0% impostor cùng chuỗi), nên nó không thể đo cổng
   T2_SEARCH. Đây là việc ưu tiên số 1.
1. ⭐ **Làm sạch nhãn ở chỗ rẻ nhất**: cụm pid liên tiếp (718/719/720/722, 758/759, 811/812,
   288/289, 203/205) + cặp **53↔119** (6.3% khối lượng, 1,392 cặp). ~20%+ khối lượng nằm ở đây.
2. **Đổi bộ chỉ số chính**: Rank-1/mAP (xếp hạng) + TAR/FAR **trong chuỗi** (cổng quyết định).
   Bỏ `TAR@FAR=0.1%` xuyên chuỗi khỏi vị trí chỉ số quyết định.
3. **Cổng ngưỡng**: cân nhắc `visual` (t\* = 0.9956, **không phải** 0.75) cho quyết định tuyệt đối;
   giữ `fused` cho xếp hạng.
4. Tăng bank tham chiếu / sửa cửa sổ (§4.1 KL7, §5) — vẫn là giả thuyết cho khoảng cách 6×.
5. **Mamba thật + train lại** (§10.2) — hướng model, nhưng **chỉ sau khi** có phép đo đúng, nếu không
   sẽ tối ưu theo metric sai lần nữa.

### ❌ Việc KHÔNG cần làm nữa

- ~~Đổi `ReIDHead` / bỏ BN~~, ~~train lại Stage 1 vì BatchNorm~~, ~~`fine_space: pre_bn`~~ — đã bác bỏ.
- ~~Train lại để "cứu embedding"~~ — Rank-1 78.15%; temporal đóng góp mAP +4.10%.
- ~~Truy tìm confound checkpoint~~ — đã loại (§6).
- ~~Tối ưu `TAR@FAR` xuyên chuỗi~~ — **sai mục tiêu** (§12.5).
- ~~Coi nhiễu nhãn là thủ phạm chính~~ — chỉ ~1–5% khối lượng (§12.2).

---

## 9. Các điểm còn tồn / cảnh báo

| # | Điểm còn tồn | Ảnh hưởng |
|---|---|---|
| 1 | Tên file plot của `calibrate_threshold.py` đã đổi thành `*_<space>.png` | `md/9thg9.md` ghi tên cũ → tài liệu lệch |
| 2 | Nhiều thay đổi hôm nay mới verify bằng `py_compile` + parse YAML (máy soạn code không có `torch`) | Số liệu §1/§1.2/§4.1 là thật (từ Colab); `rank_metrics()` đã unit-test bằng shim nhưng chưa chạy end-to-end |
| 3 | `md/model.md` đang có thay đổi chưa commit (không thuộc lần sửa này) | Cần phân biệt khi review diff |
| 4 | `infer.reid_threshold = 0.75` vs calibrated 0.852396 | Pipeline đang chạy ở điểm làm việc khác với điểm đã calibrate → số eval không tương ứng hành vi thật |
| 5 | **Chưa có cohort cố định để triển khai t-norm trong `infer.py`** | KL5 chỉ ra luật quyết định là nút thắt, nhưng hiện mới đo được *trần* (z-norm trên toàn gallery). Bước triển khai (bank tham chiếu + calibrate thang z) chưa làm |
| 6 | **Khoảng cách offline (TAR@0.75) vs online (lock ≤6.08%) chưa đo** | Nếu offline cao hơn hẳn thì cửa sổ T2_SEARCH khó hơn dữ liệu → phải sửa khâu cửa sổ. Phép đo đã thêm (KL7) nhưng chưa chạy |
| 7 | mAP chỉ 35.59% trong khi Rank-1 78.15% | Độ tương đồng *trong cùng danh tính* dao động mạnh; chưa điều tra nguyên nhân (occlusion / pha / góc nhìn) |
| 8 | **z-norm `cohort='impostor'` chưa chạy** | KL8 hiện chỉ có kết quả của biến thể thiên vị `cohort='all'` (−2.32%) → chưa thể kết luận hướng "đổi luật quyết định" |
| 9 | **Ngưỡng 0.75 chạy ở FAR 6.14%** | Sai 56× so với mục tiêu 0.1%; mâu thuẫn với `t*=0.8524` vẫn chưa chốt |
| 10 | Khoảng cách offline 38.86% vs online ≤6.08% **chưa tìm ra nguyên nhân gốc** | Đã có 3 giả thuyết (§4.1 KL7) nhưng chưa đo cái nào |

Các lần chạy **Jetson / robustness / local cũ đều phải chạy lại** — `num_frames` của chúng vừa
đổi, nên mọi kết quả cũ của chúng không còn so sánh được với kết quả mới (§0.0).

---

## 10. Nhánh TEMPORAL có thực sự đóng góp không? (câu hỏi trực tiếp 15/9)

### 10.1. Vì sao "điểm temporal thấp hơn visual" **KHÔNG** phải bằng chứng

Quan sát `temporal ≈ 0.938 < visual ≈ 0.990` **không nói được gì** về chất lượng nhánh temporal.
Đây **đúng cùng một lỗi phương pháp** đã mắc với `pre_bn` vs `fused`: cosine ở các không gian và số
chiều khác nhau **không so sánh được**. Bằng chứng phản chứng đã có: `pre_bn` (0.953) **cao hơn**
`fused` (0.734) tới 0.218, nhưng `pre_bn` lại **KÉM hơn 2.67% TAR** (§4.1 KL1–KL2). Mức điểm không
phải chất lượng phân biệt.

⇒ Phải so **khả năng phân biệt**: Rank-1 / mAP / TAR@FAR.

✅ **Đã thêm** 2 không gian vào `calibrate_threshold.py` (một lượt backbone, không tốn thêm gì):

| Space | dim | Ý nghĩa |
|---|---|---|
| `visual` | 2560 | **chỉ** nhánh visual — `model.extract_features()` trả `visual_feat` |
| `temporal` | 512 | **chỉ** token temporal (ra từ Mamba/SimpleS6Block) |
| `pre_bn` | 3072 | `cat(visual, temporal)` = đầu vào bnneck |
| `fused` | 3072 | sau `bnneck` (không gian pipeline dùng) |

và khối in mới `=== ABLATION: nhanh TEMPORAL dong gop bao nhieu? ===` với quy tắc quyết định:

- `fused ≈ visual` ở **mọi** chỉ số (ΔRank-1 < 2% **và** ΔmAP < 2%) ⇒ **temporal không đóng góp gì**.
- `fused > visual` ⇒ temporal **có** đóng góp (dù mức điểm thấp hơn).
- `fused < visual` ⇒ temporal đang **gây hại**.
- Kèm `temporal`-only so với **mốc ngẫu nhiên**: nếu Rank-1 không vượt ~3× mốc ngẫu nhiên thì nhánh
  temporal **gần như không mang thông tin danh tính**.

### 10.2. ⚠️ Confound bậc một: "Mamba" trong checkpoint này **KHÔNG** phải Mamba thật

`model.py:22-26` quyết định kiến trúc theo `HAS_MAMBA`:

```python
try:
    from mamba_ssm import Mamba
    HAS_MAMBA = True
except ImportError:
    HAS_MAMBA = False
    print("Warning: Không tìm thấy mamba_ssm. Sử dụng Simplified S6 Block (fallback).")
```

và `TemporalMambaEncoder.__init__` (`model.py:155-160`):

```python
for _ in range(num_layers):
    if HAS_MAMBA: self.layers.append(Mamba(d_model=d_model, d_state=16, d_conv=4, expand=2))
    else:         self.layers.append(SimpleS6Block(d_model=d_model))
```

Log lần chạy 15/9 **in ra đúng dòng warning đó** ⇒ **eval đang chạy `SimpleS6Block`**.

**Checkpoint cũng được train bằng `SimpleS6Block`** — chứng minh bằng shape, vì hai implementation
**khác shape** ở 2 key:

| Key | `SimpleS6Block` | `mamba_ssm.Mamba` (d_model=512, d_state=16) |
|---|---|---|
| `temporal_encoder.layers.0.x_proj.weight` | `(33, 1024)` = `d_state*2+1` | `(64, 1024)` = `dt_rank(32) + 2*d_state` |
| `temporal_encoder.layers.0.dt_proj.weight` | `(1024, 1)` | `(1024, 32)` = `dt_rank` |

Nếu checkpoint được train bằng Mamba thật, `load_checkpoint_verbose` **buộc phải** báo
`SHAPE MISMATCH` cho `temporal_encoder.layers.*.x_proj/dt_proj`. Thực tế nó chỉ báo **3 shape
mismatch, tất cả là đầu classifier**, và nạp **889/892** keys (§6.2). ⇒ **nhánh temporal khớp hoàn
toàn giữa train và eval, và cả hai đều là fallback.**

**Hệ quả:** mọi phát biểu về "Mamba" trong các tài liệu trước thực chất là về **`SimpleS6Block`**.
Điều này **không** làm kết quả sai (trọng số khớp, nhánh temporal có được train), nhưng nó có nghĩa:
muốn đánh giá **Mamba thật** thì phải `pip install mamba-ssm causal-conv1d` **VÀ TRAIN LẠI** —
`x_proj`/`dt_proj` khác shape nên **không chuyển được trọng số**. (Các key còn lại `in_proj`,
`conv1d`, `A_log`, `D`, `out_proj` thì tương thích.)

✅ **Đã thêm chẩn đoán tự động** vào `load_checkpoint_verbose`: so shape `x_proj`/`dt_proj` giữa
checkpoint và model đang chạy → in `ℹ️ Temporal encoder: cả checkpoint và model đều dùng
**SimpleS6Block (fallback)**` hoặc `❌ trọng số TEMPORAL KHÔNG khớp implementation`. Trả về
`temporal_impl_checkpoint` / `temporal_arch_ok`. Đã unit-test 4 tổ hợp train/eval × 2 impl.

### 10.3. ✅ Đã loại: temporal **không** bị đóng băng

`freeze_backbone()` (`model.py:502-506`) chỉ đặt `requires_grad = False` cho
`self.backbone.parameters()`:

```python
def freeze_backbone(self):
    for param in self.backbone.parameters():
        param.requires_grad = False
```

`self.temporal_encoder` và `self.head` **không** nằm trong `self.backbone` ⇒ **vẫn được train**.
Vậy "temporal yếu" **không** phải do nhánh temporal chạy random/đóng băng.

### 10.4. Ứng viên tối ưu MODEL (sau khi có số ablation)

| # | Ứng viên | Căn cứ | Chi phí |
|---|---|---|---|
| 1 | **Bỏ hẳn nhánh temporal** nếu ablation cho thấy nó không đóng góp | chờ số ablation | rẻ, giảm tham số |
| 2 | **Dùng Mamba thật** + train lại | §10.2 — hiện là fallback `SimpleS6Block` | phải train lại |
| 3 | **Tách layer cho 2 chiều**: `model.py:172-176` dùng **cùng một** `mamba_layer` cho cả `x` và `x.flip()` | SSM có chọn lọc (selective) mà dùng chung tham số 2 chiều thì không học được động học riêng mỗi chiều | +params, train lại |
| 4 | **Pooling visual**: `visual_feat = feats.mean(dim=1)` (`model.py:489`) là mean cứng trên N frame, kể cả frame mờ/bị che | thay bằng attention-pool / GEM / weighted-mean như `coarse_score` đang làm | train lại |
| 5 | **mAP 35.59% ≪ Rank-1 78.15%** | top-1 thường đúng nhưng **nhiều cặp genuine xếp dưới impostor** ⇒ vấn đề là **phương sai trong cùng danh tính**, không phải top-1 | xem lại loss (đã có HardTriplet sau BN+L2), PK sampler, và chất lượng cặp `before/after` trong data |

### 10.5. Về thiết kế 2 bank anchor / recent (câu hỏi 1)

Ý định thiết kế của bạn là **hợp lý**: tách `anchor` (đầu track, chất lượng cao) khỏi `recent`
(đuôi track, có thể trôi) để phòng tracking tốt đầu – kém về sau; và bộ data có bbox nhãn nên
"tracking" ở đây là hoàn hảo. Tôi **rút lại** cách gọi đó là "design smell".

Điểm tôi nêu chỉ **hẹp hơn** và vẫn đúng về mặt số: `max_anchor_size: 5` (`configs/config_colab.yaml:109`)
và `add_anchor` (`infer.py:145`) **đóng băng 5 khung ĐẦU TIÊN** rồi không bao giờ cập nhật. 5 khung
này có thể là 5 khung **đầu track** — tức xa thời điểm mất dấu nhất. Nhưng **theo yêu cầu của bạn,
việc này được xếp lại sau**: ưu tiên tối ưu model trước (§10.1–§10.4).

---

## 11. KẾT QUẢ ABLATION + nhìn thẳng vào giới hạn của suy luận (15/9 lần 3)

### 11.1. Ablation nhánh temporal — CÓ đóng góp, nhưng **không** ở điểm làm việc

Số THẬT (eval-split, 1699 pairs, mốc ngẫu nhiên Rank-1 = 2.90%):

| Space | dim | Rank-1 | mAP | TAR@FAR 0.1% | TAR@FAR 1% | FAR@t\* |
|---|---|---|---|---|---|---|
| `visual` | 2560 | 76.38% | 31.49% | **9.94%** | 23.7% | 0.1595% |
| `temporal` | 512 | 65.24% | 26.90% | 5.29% | 14.7% | 0.1021% |
| `pre_bn` | 3072 | 67.32% | 27.81% | 5.79% | 15.9% | 0.1081% |
| `fused` | 3072 | **78.15%** | **35.59%** | 8.45% | 23.2% | 0.1096% |

**Δ(`fused` − `visual`) = Rank-1 +1.77% | mAP +4.10% | TAR@FAR0.1% −1.49%**

⇒ Trả lời câu hỏi *"điểm temporal thấp hơn visual thì temporal có ý nghĩa không?"*:

1. **`temporal`-only đạt Rank-1 = 65.24% ≫ mốc ngẫu nhiên 2.90%** → nhánh temporal **CÓ** học được
   thông tin danh tính. Nó **không** vô dụng.
2. **"Điểm temporal thấp hơn" là khác biệt THANG ĐO, không phải chất lượng** — đúng cùng loại lỗi
   đã mắc với `pre_bn` (0.953) vs `fused` (0.734) ở §4.1 KL2.
3. ⭐ **Phát hiện quan trọng: temporal giúp XẾP HẠNG nhưng làm HẠI ĐIỂM LÀM VIỆC.** `fused` hơn
   `visual` về mAP (+4.10%) và Rank-1 (+1.77%) — tức temporal mang thêm thông tin — **nhưng** lại
   KÉM hơn về TAR@FAR 0.1% (−1.49%). Fusion (concat + BN) **mở rộng đuôi impostor nhiều hơn** so
   với mức nó mở rộng phân bố genuine.
4. ⇒ **`visual`-only (9.94%) là TAR@FAR 0.1% TỐT NHẤT trong cả 4 không gian.** Hành động rẻ nhất:
   dùng `visual` cho **cổng quyết định theo ngưỡng tuyệt đối**, và/hoặc **fusion ở mức ĐIỂM**
   (2 cổng độc lập / học trọng số) thay vì concat rồi BN.

### 11.2. z-norm: giả thuyết "luật quyết định" — **BỊ BÁC BỎ**

| Space | cohort=all | cohort=impostor |
|---|---|---|
| `fused` | 6.13% (−2.32%) | 7.27% (−1.18%) |
| `pre_bn` | 4.78% (−1.01%) | 5.65% (−0.13%) |
| `visual` | 4.65% (−5.29%) | 5.19% (−4.75%) |
| `temporal` | 4.63% (−0.67%) | **5.51% (+0.22%)** |

Bản `cohort='impostor'` (upper bound đúng, μ/σ chỉ trên cột khác danh tính) **cải thiện so với bản
`all`** nhưng **tốt nhất chỉ +0.22%**. Kết luận:

❌ **Chuẩn hoá điểm theo từng truy vấn KHÔNG phải là cách sửa.** Điểm cosine **đã** tương thích giữa
các truy vấn; giả thuyết "mức điểm lệch nhau" (§4.1 KL5, §7 hàng 4) **bị bác bỏ**.

### 11.3. Vì sao tôi "đoán" được, và tôi **không** đoán được cái gì

Câu hỏi rất đúng. Phải tách 3 loại khác nhau:

**(A) SUY RA ĐƯỢC từ code/số liệu — không phải đoán:**

| Kết luận | Vì sao là suy luận, không phải phỏng đoán |
|---|---|
| `pre_bn` sẽ không thắng `fused` | `HardTripletLoss` được áp **sau BN + L2** (`train_reid.py:468-471`) — đây là **sự kiện đọc được trong code**, không phải trực giác. Vẫn **khả sai**: nếu `pre_bn` thắng thì kết luận chết. |
| Cảnh báo `❌` backbone là sai | **Kiểm chứng được** bằng cách đọc `ReIDHead.forward`. Tôi sai vì **viết cảnh báo TRƯỚC khi đọc**, tức lỗi **thứ tự suy luận**, không phải "đoán sai". |
| Không được so MỨC điểm giữa các không gian | Suy ra từ **định nghĩa phép đo** (cosine ở số chiều/không gian khác nhau), và có **phản chứng trong chính bộ số này** (`pre_bn` 0.953 > `fused` 0.734 nhưng `pre_bn` kém hơn). |

**(B) TÔI ĐÃ ĐOÁN VÀ ĐÃ SAI** — phải nói thẳng:

- Tôi dự đoán z-norm có thể nâng TAR **mạnh** ("trần của hướng đổi luật quyết định"). Đo được:
  **+0.22%**. **Dự đoán của tôi SAI.**
- Tôi xếp "luật quyết định" là **nghi phạm chính** (§7 hàng 4). Nay **bị bác bỏ** (§11.2).
- Tôi nghi bank neo quá nhỏ (§4.1 KL7 giả thuyết a) — **chưa kiểm chứng gì cả**.

**(C) TÔI KHÔNG ĐOÁN ĐƯỢC GÌ — và không được giả vờ:**

Câu hỏi *"lỡ có nguyên nhân khác thì sao"* là đúng chỗ. **TAR@FAR thấp + Rank-1 khá là một chẩn đoán
KHÔNG đặc hiệu**: nó khớp với **nhiều** nguyên nhân khác nhau, và số liệu hiện có **không** phân biệt
được chúng:

| Nguyên nhân khả dĩ | Có khớp với số liệu? | Đã kiểm chứng? |
|---|---|---|
| Nhiễu nhãn / **trùng danh tính** (cùng drone mang 2 `identity_id`, hoặc frame gần trùng) | ✅ rất khớp: vài cặp "impostor" thực ra là **cùng vật** → điểm rất cao → đẩy phân vị 99.9% lên **0.852** → TAR sụp ở FAR 0.1%. **Và z-norm không thể sửa nhãn sai** | ❌ **CHƯA** |
| Cặp `before/after` dương bị lệch (occlusion, góc nhìn) → mAP thấp | ✅ khớp (mAP 35.6% ≪ Rank-1 78%) | ❌ chưa |
| Domain gap backbone (DINOv3 pretrain trên ảnh tự nhiên, target là drone nhỏ) | ✅ khớp | ❌ chưa |
| Train chưa đủ / loss chưa phù hợp | ✅ khớp | ❌ chưa |
| Cửa sổ T2_SEARCH / bank (KL7) | ✅ khớp khoảng cách 6× | ❌ chưa |

⇒ **Những gì tôi có là các PHÉP LOẠI TRỪ, không phải sự QUY KẾT.** Đã loại chắc: Mamba, BatchNorm1d,
confound checkpoint, và (nay) chuẩn hoá điểm. **Phần khoảng cách còn lại thì CHƯA quy được cho
nguyên nhân nào.** Nói "gốc rễ là X" lúc này là đi quá số liệu.

**Phép thử phân biệt cho nghi phạm hàng đầu (nhiễu nhãn/trùng danh tính) — rẻ nhất, nên làm ngay:**

> In ra **các cặp impostor có điểm cao nhất** (ví dụ top 50 cặp vượt 0.852) kèm `identity_id`,
> `sequence_id`, đường dẫn ảnh, và **khoảng cách frame**. Nếu chúng là **cùng một drone** mang 2
> nhãn khác nhau, hoặc frame gần trùng của cùng một chuỗi → **nhiễu nhãn/trùng lặp là thủ phạm**,
> và mọi kết luận "model yếu" đều phải xem lại. Nếu chúng là các drone **khác nhau** thật → giả
> thuyết này bị loại và quay về chất lượng embedding/dữ liệu.

Đây là **phép đo quyết định còn thiếu**, và nó **rẻ hơn** cả việc chạy lại calibration.

### 11.4. Ngưỡng 0.75 chỉ thuộc thang đo của `fused` — minh chứng định lượng

| Space | t\* calibrate cho FAR 0.1% | TAR tại 0.75 | FAR tại 0.75 |
|---|---|---|---|
| `fused` | 0.852396 | 38.86% | 6.1393% |
| `pre_bn` | 0.971264 | 100.00% | 99.9979% |
| `visual` | 0.995573 | 100.00% | **100.0000%** |
| `temporal` | 0.962562 | 99.94% | 99.7494% |

`visual` có t\* = **0.9956** — toàn bộ phân bố điểm nằm quanh 0.99, nên ngưỡng **0.75 chấp nhận 100%**
mọi cặp. Đây là minh chứng dứt khoát cho §2: *"`raw` vượt 0.75 ở 18/18 event"* **không có nghĩa gì cả**
— mỗi không gian có **thang đo riêng**, và **bắt buộc phải calibrate ngưỡng theo từng không gian**.

⚠️ **Hệ quả thực hành:** nếu chuyển cổng quyết định sang `visual` (đang là TAR@FAR0.1% tốt nhất,
9.94%), **phải dùng t\* = 0.9956**, không được giữ 0.75.

### 11.5. Hai lỗi đã sửa trong lần chạy này

| Lỗi | Biểu hiện | Sửa |
|---|---|---|
| Đọc sai cấu trúc `far_curve` | Cột `TAR@FAR1%` trong bảng ablation in **`nan%`** | `far_curve` là **dict theo space** (`far_curve[s]`), không nằm trong `results[s]`. Đã đổi `r.get('far_curve')` → `far_curve.get(s, [])` |
| Verdict lỗi thời | In `"CA HAI khong gian..."` (hardcode, dù có 4 không gian) và khẳng định **`"doi LUAT QUYET DINH"`** — thứ đã bị khối z-norm bác bỏ ngay bên dưới | Giờ đếm số không gian động, lấy space tốt nhất động, và **không kết luận thay**: nêu 2 nhánh (a)/(b) kèm phép thử tương ứng |

### 11.6. Thứ tự việc cần làm (chốt)

1. ✅ **ĐÃ LÀM — phép thử nhiễu nhãn / trùng danh tính** (§11.7). Chạy lại
   `python calibrate_threshold.py --config configs/config_colab.yaml` (KHÔNG cần đổi gì) và đọc khối
   `=== CHAN DOAN NHIEU NHAN / TRUNG DANH TINH ===` ở gần cuối. **Việc cần làm bây giờ là ĐỌC khối
   đó và MỞ ẢNH** ở các đường dẫn nó in ra.
2. **Thử `visual`-only cho cổng ngưỡng** (với t\* = 0.9956, không phải 0.75) — rẻ, và là TAR@FAR0.1%
   tốt nhất trong 4 không gian.
3. **Fusion ở mức điểm** (2 cổng độc lập / học trọng số) thay vì concat rồi BN — vì `fused` hơn về
   xếp hạng nhưng kém về điểm làm việc (§11.1).
4. **Mamba thật + train lại** (§10.2) nếu muốn cải thiện chất lượng embedding — đây là hướng
   *model*, không phải pipeline.
5. Sửa cửa sổ / bank (§5, §4.1 KL7) — hướng *pipeline*, xếp sau theo yêu cầu.

### 11.7. ✅ ĐÃ THÊM: chẩn đoán nhiễu nhãn / trùng danh tính

`calibrate_threshold.py` giờ in khối `=== CHAN DOAN NHIEU NHAN / TRUNG DANH TINH ===` (gần cuối) và
lưu vào `calibration_report.json` khoá `label_noise_diagnosis`. Hàm: `diagnose_top_impostors()`.

**Ba đầu ra:**

| # | Đầu ra | Trả lời câu gì |
|---|---|---|
| A | `top_pairs` — 10 cặp impostor điểm cao nhất: `score`, `pid` hai bên, `sequence_id` hai bên, `same_sequence`, `event_index`, số frame, **đường dẫn ảnh**, cờ `above_threshold` | Mở ảnh ra xem: **cùng một drone hay hai drone khác nhau?** |
| B | `top_pid_pairs` — các **cặp danh tính** dễ nhầm nhất theo `mean_sim` | Nếu một cặp `identity_id` **khác nhau** có `mean_sim` ~0.9 (bằng mức genuine) ⇒ **nhiễu nhãn gần như chắc chắn** |
| C | `concentration` — khối lượng impostor vượt `t*` tập trung vào bao nhiêu cặp danh tính, và tỉ lệ **cùng chuỗi / khác chuỗi** | Nếu **1–2 cặp pid** chiếm phần lớn khối vượt ngưỡng ⇒ ngưỡng bị đặt bởi một lỗi cục bộ, không phải do embedding yếu toàn cục |

**Cách diễn giải (ghi trong cả code, tránh kết luận sai):**

| Quan sát | Kết luận |
|---|---|
| `same_sequence=True` | Distractor **khác danh tính trong CÙNG chuỗi** = **hard negative hợp lệ** (đúng loại pipeline gặp). **KHÔNG** phải nhiễu nhãn. |
| `same_sequence=False` + ảnh giống hệt | **Nghi trùng danh tính / nhiễu nhãn** → phải xem ảnh để xác nhận |
| Cặp pid khác nhau có `mean_sim` ~0.9 | **Nhiễu nhãn gần như chắc chắn** |
| Top cặp là drone **khác nhau thật** | Giả thuyết nhiễu nhãn **BỊ LOẠI** → quay về chất lượng embedding/dữ liệu |

Đã unit-test bằng dữ liệu giả **có nhãn sai biết trước**: hàm phát hiện đúng cặp nhiễu (`mean_sim`
0.943 so với ~0.04 của các cặp sạch), và báo `KHÁC chuỗi = 100%` — đúng chữ ký của trùng danh tính
chứ không phải hard negative cùng chuỗi.

> ⚠️ **Chưa có kết luận nào về nguyên nhân cho tới khi khối này chạy trên dữ liệu thật và các đường
> dẫn ảnh được mở ra xem.** Cặp pid `0 <-> 1` trong test là do tôi tạo ra; số thật thì chưa biết.

---

## 12. KẾT LUẬN TỪ CHẨN ĐOÁN NHIỄU NHÃN + AUDIT DATASET (15/9 lần 4)

### 12.1. ⭐ PHÁT HIỆN CẤU TRÚC QUYẾT ĐỊNH: giao thức eval không khớp bài toán pipeline

`audit_identity_sequences.py` trên `query_test.json`/`gallery_test.json`:

| Chỉ số | Giá trị |
|---|---|
| Số `identity_id` | **340** |
| Số `sequence_id` | **340** |
| Số event | 5,260 (2,630 query + 2,630 gallery) |
| `#sequence` mỗi identity | **1** (100.0%) |
| `#identity` mỗi sequence | **1** (100.0%) |
| impostor **CÙNG chuỗi** | **0** (0.0%) |
| impostor **KHÁC chuỗi** | **6,810,772** (100.0%) |

**`identity_id` ↔ `sequence_id` là SONG ÁNH.** Mỗi sequence đúng 1 danh tính, mỗi danh tính đúng 1 sequence.

⇒ **Toàn bộ ngưỡng FAR 0.1% được calibrate trên 6.8 triệu cặp XUYÊN CHUỖI giữa 340 drone
KHÁC NHAU.** Pipeline **không bao giờ** so sánh xuyên chuỗi — nó so cửa sổ hiện tại với bank của
**chính target trong CÙNG chuỗi**.

⇒ **`TAR@FAR = 8.45%` trả lời một câu hỏi KHÁC với câu hỏi mà cổng T2_SEARCH cần.** Con số này
không sai, nhưng nó **không phải metric quyết định** cho bài toán re-acquisition. Đây là lời giải
cho nghịch lý "Rank-1 78% mà TAR@FAR 8.45%": hai chỉ số đang đo hai bài toán khác nhau.

⚠️ Hệ quả: trong bộ JSON này, **âm (impostor) mà pipeline thực sự gặp — khoảng thời gian target
VẮNG MẶT — không hề có trong dữ liệu**. Không thể đo cổng T2_SEARCH bằng protocol hiện tại.

### 12.2. Nhiễu nhãn / phân mảnh danh tính: CÓ THẬT nhưng chỉ là phần NHỎ

**Bằng chứng trực tiếp (Nhóm 2 của bạn):**

| Cặp pid | score | Verdict của bạn |
|---|---|---|
| 719 → 720 | 0.9557 và 0.9320 | **"cùng 1 cái"** — xuất hiện 2 lần trong top 10 |
| 63 → 108 | 0.9366 | "2 UAV khác nhau" |
| 108 → 237 | 0.9327 | "2 UAV khác nhau" |
| 6 cặp còn lại | 0.93–0.97 | "nhìn giống nhau" (không phân biệt được instance) |

Sequence của 719/720 là `20190925_124612_1_1` vs `20190925_124612_1_2` — **cùng một phiên ghi hình
bị tách thành 2 sequence_id** ⇒ cùng drone bị gán 2 nhãn. **Đây là phân mảnh danh tính thật.**

**Chữ ký thống kê (tôi kiểm tra riêng):** trong 10 cặp pid dễ nhầm nhất, **7 cặp có `|Δpid| ≤ 3`**
(719/720, 718/719, 719/722, 758/759, 811/812, 288/289, 203/205). Nếu pid được đánh ngẫu nhiên theo
drone thì xác suất `|Δpid| ≤ 3` trong 340 id chỉ ~**1.8%** ⇒ **làm giàu ~39×**.
⇒ **pid LIÊN TIẾP ≈ cùng phiên ghi hình**, tức quy ước đánh số đã tách một drone thành nhiều nhãn.

**Nhưng quy mô nhỏ:**

| Đại lượng | Giá trị |
|---|---|
| Cặp impostor vượt ngưỡng | 3,070 |
| Cặp 719↔720 | 16 cặp |
| Cụm 718/719/720/722 | ~28 cặp ≈ **0.9%** |
| Số cặp pid góp vào khối vượt ngưỡng | **493** / 20,706 |
| Share của cặp lớn nhất (53↔119) | **6.3%** |
| Share của top 5 cộng lại | ~22% |

⇒ Khối lượng vượt ngưỡng **PHÂN TÁN**, không tập trung vào 1–2 cặp. **Nhiễu nhãn giải thích được
cỡ ~1–5%**, không phải phần chính. ⇒ Giả thuyết "nhiễu nhãn là thủ phạm chính" **BỊ LOẠI**.

### 12.3. Phần CHÍNH là nhầm lẫn FINE-GRAINED giữa các drone KHÁC NHAU thật

- **63↔108 (0.9366) và 108↔237 (0.9327) — bạn xác nhận là 2 UAV KHÁC NHAU — đều VƯỢT ngưỡng.**
  Đây là bằng chứng trực tiếp: có cặp impostor điểm cao mà **không phải** lỗi nhãn.
- Cặp đóng góp lớn nhất **53↔119**: 1,392 cặp, `mean=0.7918`, 6.3% khối vượt ngưỡng — `|Δpid|=66`,
  **không liên tiếp** ⇒ look-alike thật, không phải phân mảnh nhãn. (pid 53 còn xuất hiện ở 3 trong
  top 5: 53↔119, 53↔250, 53↔108 ≈ 14% khối lượng.)
- 6/10 cặp "nhìn giống nhau" mà bạn không phân biệt được instance ⇒ đúng loại
  **same-model / different-instance**.
- Khớp với **mAP 35.59% ≪ Rank-1 78.15%**.

⇒ Trội là **bài toán fine-grained**: phân biệt các drone **cùng loại**. Đây là **hạn chế thật của
embedding**, không phải lỗi nhãn — nhưng nó bị **giao thức xuyên-chuỗi phóng đại** lên.

### 12.4. Trả lời câu hỏi "gốc rễ là gì" — bảng cập nhật

| Nghi phạm | Trạng thái | Bằng chứng |
|---|---|---|
| Mamba / temporal | ❌ loại | temporal-only Rank-1 65.24% ≫ 2.90%; đóng góp mAP +4.10% |
| BatchNorm1d | ❌ loại | `pre_bn` kém hơn 2.67% TAR |
| Confound checkpoint | ❌ loại | 889/892 keys; 3 key thiếu là đầu classifier (§6) |
| Luật quyết định / chuẩn hoá điểm | ❌ loại | z-norm tốt nhất +0.22% (§11.2) |
| **Nhiễu nhãn là thủ phạm CHÍNH** | ❌ **loại** | có thật nhưng chỉ ~1–5% khối lượng (§12.2) |
| **Giao thức eval không khớp deployment** | ⭐⭐ **ĐÃ XÁC NHẬN** | 100% impostor là xuyên chuỗi; 0% cùng chuỗi; pipeline làm việc trong 1 chuỗi (§12.1) |
| **Fine-grained: drone cùng loại khó phân biệt** | ⭐⭐ **ĐÃ XÁC NHẬN (một phần)** | 2 cặp KHÁC NHAU bạn xác nhận vượt ngưỡng; mAP 35.6% ≪ Rank-1 78% (§12.3) |
| Cửa sổ T2_SEARCH / bank | ⚠️ chưa kiểm chứng | offline 38.86% vs pipeline ≤6.08% (§4.1 KL7) |

### 12.5. Việc cần làm — ĐỔI MỤC TIÊU ĐO

**Kết luận quan trọng nhất: tối ưu theo `TAR@FAR` xuyên chuỗi là TỐI ƯU SAI MỤC TIÊU.** Model không
hỏng (Rank-1 78%, temporal đóng góp thật), nhưng nó đang bị chấm bằng một bài toán khác.

1. ⭐⭐⭐ **Dựng validation KHỚP deployment (trong cùng chuỗi).** Dùng metadata đã có trong
   `query_test.json` (`disappearance_duration_frames`, `event_index`):
   - **Dương**: cửa sổ sau khi tái xuất (target hiện rõ) → **PHẢI** lock
   - **Âm**: cửa sổ trong khoảng target **VẮNG MẶT** → **KHÔNG ĐƯỢC** lock
   Đây mới là TAR/FAR mà cổng T2_SEARCH phải tối ưu. Hiện protocol **không có** tập âm này.
2. ⭐ **Kiểm tra cụm pid liên tiếp** (718/719/720/722, 758/759, 811/812, 288/289, 203/205) và
   **53↔119** — đây là ~20%+ khối lượng và là chỗ rẻ nhất để làm sạch nhãn. Nếu 53↔119 cũng là cùng
   một drone thì phần "look-alike thật" thu hẹp đáng kể; nếu khác thì xác nhận fine-grained.
3. **Bỏ ưu tiên `TAR@FAR=0.1%` làm chỉ số chính.** Thay bằng: Rank-1/mAP (xếp hạng) + TAR/FAR
   **trong chuỗi** (cổng quyết định). Hai mục tiêu khác nhau, cần hai chỉ số.
4. Giữ `fused` cho xếp hạng, và cân nhắc `visual` cho **cổng ngưỡng** (t\* = 0.9956, không phải 0.75)
   — vì ở FAR xuyên chuỗi `visual` tốt hơn, còn temporal giúp xếp hạng trong chuỗi.

---

## 13. VÌ SAO `num_frames` LÚC INFER PHẢI KHÁC LÚC TRAIN (15/9 lần 5)

Nguồn: `md/test.txt` (6 run, **cùng 1 video** `UAV-Anti-UAV_Test_000059`, chỉ đổi backbone + `num_frames`).

### 13.1. Số liệu — phải lấy DEBUG ĐẦU TIÊN của mỗi chu kỳ

⚠️ **Bẫy số liệu:** khi fine FAILED, pipeline in `DEBUG` **mỗi frame** (rolling window). Trung bình toàn bộ
dòng DEBUG bị lệch nặng (run 1.1 chỉ 7 dòng, run 1.2 tới 218 dòng → run tệ nhất lại được trọng số lớn
nhất). Số dưới đây lấy **DEBUG đầu tiên của mỗi chu kỳ** = đúng khoảnh khắc quyết định.

| Run | Backbone | N_train | **N_infer** | Chu kỳ có quyết định | **fine OK** | **temporal** | visual_plain | fused |
|---|---|---|---|---|---|---|---|---|
| 1.1 | resnet50-ibn | 12 | **8** | 6/8 | **5** | **0.977** | 0.990 | 0.829 |
| 1.2 | resnet50-ibn | 12 | 12 | 4/8 | 1 | **0.349** ⬇ | 0.990 | 0.318 |
| 1.3 | resnet50-ibn | 12 | 16 | 3/8 | 1 | 0.680 | 0.991 | 0.459 |
| 2.1 | dinov3-convnext | 8 | 8 | 6/8 | 1 | **0.579** ⬇ | 0.986 | 0.157 |
| 2.2 | dinov3-convnext | 8 | 12 | 4/8 | 3 | 0.939 | 0.990 | 0.747 |
| 2.3 | dinov3-convnext | 8 | **16** | 3/8 | **3** | **0.958** | 0.994 | 0.849 |

### 13.2. Ba sự thật được SỐ LIỆU CHỨNG MINH

**(1) Nhánh thị giác MIỄN NHIỄM với `num_frames`.** `visual_plain` ∈ [0.986, 0.994] ở **cả 6 run**, kể cả
run tệ nhất. Cùng video, cùng frame, cùng bbox ⇒ **khung hình/bbox/backbone KHÔNG phải vấn đề.**

**(2) Chỉ nhánh TEMPORAL sụp, và sụp NGAY TẠI `N = N_train`:**
- resnet (train 12): 0.977 @8 → **0.349 @12** → 0.680 @16
- dinov3 (train 8): **0.579 @8** → 0.939 @12 → 0.958 @16

Cùng video, cùng event, cùng bank, **chỉ đổi N** ⇒ đây là **hiệu ứng thuần theo ĐỘ DÀI chuỗi**, không
phải do nội dung khung hình. Và trong **cả hai** backbone, N tệ nhất **chính là N đã train**.

**(3) `num_frames` còn điều khiển SỐ CƠ HỘI quyết định — thuần cơ học.** Số chu kỳ có quyết định =
**6, 4, 3** cho N=8, 12, 16 — **giống hệt nhau ở cả hai backbone**, vì `SlidingWindowBuffer.is_ready()`
(infer.py:63) cần đủ `window_size` frame, mà cửa sổ tái xuất trong video này rất ngắn
(ví dụ cycle 1 của run 1.2: chỉ có frame 101–108 = 8 frame < 12 → **không bao giờ đủ để ra quyết định**,
log ghi thẳng `[109] UAV lost during T2_SEARCH`).

⇒ "N tối ưu" thực chất là argmax của **tích** (số cơ hội) × (chất lượng mỗi lần):
- resnet: N=8 vì N=12/16 làm temporal sụp → mất cả cơ hội lẫn chất lượng → **5 lock**
- dinov3: N=16 vì N=8 làm temporal sụp (0.579); N=16 tuy chỉ 3 cơ hội nhưng **3/3 thành công** → **3 lock**

### 13.3. Chuỗi nhân quả (bám code)

**(a) Train và Infer dựng chuỗi temporal theo HAI cách khác nhau** — đây là gốc:
- **Train** (`train_reid.py:172`): `np.linspace(0, len-1, num_frames)` → `num_frames` frame **trải đều
  qua CẢ event** (event 100+ frame) ⇒ **bước thời gian LỚN**, nội dung đa dạng.
- **Infer** (`infer.py:248`): `SlidingWindowBuffer(num_frames, stride=1)` ⇒ `num_frames` frame
  **LIÊN TIẾP** ⇒ **bước thời gian = 1**, các frame gần như trùng nhau.

⇒ Đầu vào temporal ở infer **nằm ngoài phân phối train ở MỌI N**. Khớp `N = N_train` chỉ khớp **số
lượng**, không khớp **động học**. Đây là lý do "dùng đúng N lúc train lại tệ".

**(b) `TemporalMambaEncoder` không bất biến theo độ dài — theo cấu tạo.** Bốn chỗ phụ thuộc N:
| Chỗ | Code | Hệ quả |
|---|---|---|
| `mean(dim=1)` | `model.py:184` | Lấy trung bình một lượng **quỹ đạo khác nhau** |
| `conv1d(kernel=4, padding=3)` causal | `model.py:44-51` | 3/N vị trí đầu bị nhiễu zero-pad |
| `mamba(x) + mamba(x.flip())` **dùng CHUNG layer** | `model.py:174-176` | Ở vị trí i, ghép "quá khứ cụt" với "tương lai cụt"; tỉ lệ biên ~k/N |
| `P = cumsum(W)`; `weights = exp(P_i - P_j)` | `model.py:107,119` | `P` có độ lớn ~O(N) ⇒ hiệu `P_i - P_j` mất độ chính xác float32 khi N lớn |

**(c) `out_mlp`'s BatchNorm1d là bộ KHUẾCH ĐẠI.** BN (`model.py:157-161`) dùng running stats fit ở
`N_train`. Lệch nhỏ về scale/shift của `mean_i(h_i)` bị BN chuẩn hoá lại rồi **ReLU cắt** ⇒ nhiều kênh
đổi dấu ⇒ **hướng** vector 512-d đổi mạnh hơn nhiều so với độ lớn.

**(d) `bnneck` lan lỗi temporal sang `fused`.** BN **theo từng kênh** san phẳng ưu thế biên độ của khối
visual (2560 chiều) ⇒ **nâng trọng số tương đối của 512 chiều temporal** trong cosine fused.
Bằng chứng: `corr(temporal, fused) = 0.89` trên 6 run, và `BN delta = raw − fused` **tỉ lệ nghịch** với
`temporal` (0.349 → delta ~0.63; 0.977 → delta ~0.15).

### 13.4. Trả lời trực tiếp câu hỏi

> **"Vì sao resnet thích N=8 còn dinov3 thích N=16?"**

Vì **cả hai đang chọn N để TRÁNH vùng encoder temporal sụp**, chứ không phải vì N đó "đúng" về mặt
vật lý. Vùng sụp lại nằm **ngay tại N đã train**, nên hai checkpoint có N_train khác nhau sẽ có N tối ưu
khác nhau, theo hai hướng ngược nhau:
- resnet N_train=12 → 8 và 16 đều "xa" vùng sụp, và **8 thắng** vì còn giữ được 6 cơ hội quyết định.
- dinov3 N_train=8 → **8 là đáy sụp**, phải đi lên; **16 thắng** dù chỉ còn 3 cơ hội, vì 3/3 thành công.

⇒ **`num_frames` ở infer đang là một "nút bịt" chứ không phải siêu tham số.** Đây là **triệu chứng của
lỗi huấn luyện**, không phải thứ cần tune. Hệ quả thực tế: chọn N=8 cho resnet và N=16 cho dinov3 là
**đúng cho checkpoint hiện tại nhưng sẽ vỡ ở lần train lại kế tiếp.**

### 13.5. Việc cần làm

1. ⭐⭐⭐ **Làm train và infer dùng CÙNG ngữ nghĩa cửa sổ.** Hai lựa chọn:
   - **(A) Sửa ở infer (rẻ, không cần train lại):** cho `SlidingWindowBuffer` dùng stride thích ứng để
     **khoảng thời gian** của cửa sổ khớp lúc train: `stride ≈ round(span_infer / span_train)`.
   - **(B) Sửa ở train (đúng hơn):** train với `stride=1` + đúng `num_frames` sẽ dùng lúc infer, **và
     randomize N** (ví dụ N ∈ {8,12,16} mỗi batch) để encoder **bất biến theo độ dài**.
2. ⭐⭐ **Cắt phụ thuộc của cổng vào nhánh hỏng.** Nhánh `visual` **miễn nhiễm hoàn toàn** với
   `num_frames` (0.986–0.994 ở mọi run). Dùng **cổng trên `visual`** (t\* = 0.9956, §11) hoặc fusion ở
   mức điểm ⇒ cổng **không còn phụ thuộc N**. Đây là hành động rẻ nhất và ăn khớp với §11 (ablation
   cho thấy `visual` tốt nhất ở FAR cố định).
3. ⭐ **`mamba_ssm` thật + train lại.** Log cảnh báo rõ checkpoint đang dùng `SimpleS6Block` (fallback).
   Block này tính scan bằng `cumsum` + `exp` — **nhạy độ dài theo cấu tạo**. Mamba thật dùng recurrence
   `dt`-discretized, tổng quát hoá độ dài tốt hơn nhiều. **Rất có thể phần lớn hiện tượng §13 là artifact
   của fallback.**
4. **Đo lại metric cho đúng:** đếm **số lock trên mỗi event** (tính cả trường hợp "không đủ frame để ra
   quyết định"), không chỉ đếm điểm trung bình — vì `num_frames` làm mất cơ hội một cách cơ học.

### 13.6. Phép đo còn thiếu (chưa kết luận)

Tôi **chưa** giải thích được vì sao N tệ nhất lại **đúng bằng** N_train (chứ không chỉ "không tốt nhất").
Cần đo trực tiếp trên checkpoint — snippet 10 dòng, chạy trên **một cửa sổ cố định**:

```python
seq = window.get_sequence()                    # [1, N, 2560] — CÙNG nội dung
for N in (8, 12, 16):
    s = seq[:, :N]                             # cắt cùng cửa sổ, chỉ đổi N
    pe = model.temporal_encoder.pos_embed[:, :N, :]
    t, tseq = model.temporal_encoder(s)
    print(N, '|token|=%.3f' % t.norm(), '|meanPE|=%.3f' % pe.mean(1).norm(),
          '|mean(h)|=%.3f' % tseq.mean(1).norm(),
          'cos(t8, tN)=%.3f' % F.cosine_similarity(t8, t, dim=-1).item())
```
- Nếu `|mean(h)|` hoặc `|token|` **đổi mạnh theo N** → xác nhận BN/scale là bộ khuếch đại.
- Nếu `|meanPE|` đổi mạnh theo N → PE (nhất là các vị trí **chưa được train** khi N > N_train) là thủ phạm.
- Nếu mọi norm đều ổn mà `cos` vẫn sụp → thủ phạm nằm trong `cumsum/exp` của `SimpleS6Block`.
