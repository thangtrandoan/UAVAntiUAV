# Đồng bộ `frame_stride` toàn pipeline + sửa lỗi stride của soft lock

Ngày: 22/9/2026
Liên quan: `md/15thg9.md` (§13 — vì sao `num_frames` infer phải khác lúc train), `md/14thg9.md`

> **Tóm tắt 1 dòng:** đã đồng bộ **bước thời gian** giữa data → train → infer về đúng `frame_stride = 4`,
> và trên đường đi phát hiện **hai lỗi thật**: (1) anchor và query được dựng với **mật độ lấy mẫu
> khác nhau**, (2) `soft_lock_buffer.stride` là **dead code** — gán nhưng **chưa bao giờ được đọc**,
> nên bản sửa đầu tiên của tôi **không có tác dụng gì**.

---

## 0. Đã làm gì hôm nay (changelog)

| # | File | Thay đổi | Vì sao |
|---|---|---|---|
| 1 | `train_reid.py` | `_load_clip` **BỎ `np.linspace`** → cắt **contiguous**; thêm tham số `take_last` | `np.linspace` lấy mẫu **trải đều cả danh sách** → bước thời gian hiệu dụng `frame_stride × (len−1)/(num_frames−1)` ≈ **5.45** thay vì 4 → train lệch infer |
| 2 | `train_reid.py` | `__getitem__`: `take_last=True` cho gallery/before, `False` cho query/after | before lấy `num_frames` frame **CUỐI** (sát `t1` lúc mất dấu); after lấy **ĐẦU** (sát `t2` lúc tái xuất) |
| 3 | `evaluate_reid.py` | Cùng #1 + #2 | Offline eval phải dùng **cùng phân phối temporal** với train |
| 4 | `calibrate_threshold.py` | Cùng #1 + #2 | Calibration phải dùng cùng phân phối |
| 5 | `infer.py` | `main()`: đọc `data_pipeline.frame_stride` và **GHI ĐÈ** `infer.stride`, cảnh báo nếu lệch | `infer.stride` trước đây hardcode 2 → không thể trôi lệch được nữa |
| 6 | `infer.py` | `SlidingWindowBuffer`: thêm `get_strided_sequence(stride=1)` và `get_weighted_visual_mean(stride=1)` | Cần lấy **cách quãng** ra khỏi buffer thu liên tục |
| 7 | `infer.py` | `compute_fused_vector(model, window, stride=1)` | HARD LOCK truyền `frame_stride`; tracking để mặc định 1 (vì `sliding_window` **đã** cách quãng sẵn) |
| 8 | `infer.py` | **PHÂN VAI stride**: `soft_lock_buffer` thu **LIÊN TỤC** (`stride=1`), sức chứa `(num_frames−1)×stride + 1` | Theo yêu cầu: **soft lock lấy liên tục, HARD LOCK mới lấy cách quãng** |
| 9 | `infer.py` | T2_SEARCH: HARD LOCK gọi `compute_fused_vector(..., stride=self.stride)` | Cửa sổ temporal của truy vấn có bước = `frame_stride`, **khớp Memory Bank và khớp lúc train** |
| 10 | `infer.py` | Khi HARD LOCK thành công: nạp `sliding_window` từ **đúng bản cách quãng** đã dùng để lock | Trước đây gán cả buffer thu liên tục làm `sliding_window` → anchor sai bước thời gian |
| 11 | `infer.py` | Fine FAILED: trượt **đúng 1 bước thời gian** (`stride` frame) thay vì `pop(0)` | `pop(0)` làm cửa sổ kế tiếp gần như **trùng** cửa sổ vừa kiểm tra → rolling window **vô hiệu** |
| 12 | `infer.py` | Last-moment update: **BỎ pad nhân bản frame cuối** | Pad nhân bản tạo **bước thời gian = 0** (chuyển động giả "đứng yên") |
| 13 | `infer.py` | `draw_ui`: hiển thị `len/soft_lock_capacity` thay vì `len/num_frames` | Sức chứa đã đổi |
| 14 | `evaluate_reid_robustness.py` | Cùng #6–#13 | Cùng thiết kế |
| 15 | `evaluate_reid_robustness.py` | `sample_imposters(..., stride)`: lấy 1 frame mỗi `stride`; `valid_starts` trải `num_frames × stride` | Trước đây lấy 12 frame **liền nhau** → impostor có bước thời gian khác genuine |
| 16 | `phan_rang/infer_realworld.py` | Cùng #6–#13 | Cùng thiết kế |
| 17 | `configs/config_colab.yaml` | Thêm `data_pipeline.frame_stride: 4`; `infer.stride: 2 → 4`; `infer_robustness.stride: 4` | `frame_stride` thành **nguồn duy nhất** |
| 18 | `configs/config_local.yaml` | `infer.stride: 2 → 4` | Đã có sẵn `frame_stride: 4` |
| 19 | `configs/config_jetson.yaml` | Thêm `frame_stride: 4`; `infer` / `infer_robustness` / `infer_realworld` stride `2 → 4` | Cùng #17 |
| 20 | `configs/config_jetson_smoke.yaml` | Thêm `frame_stride: 4` | Cùng #17 |

> `configs/config_h100.yaml` **không đụng** (theo yêu cầu từ trước).

---

## 1. Bug 1a — anchor và query được dựng với **mật độ lấy mẫu khác nhau**

### 1.1. Phát hiện

`Memory Bank` (anchor) được dựng từ `sliding_window`, thu qua `should_extract()` → **bước = `frame_stride`**.
Nhưng truy vấn ở T2_SEARCH được dựng từ `soft_lock_buffer` → **bước = 1**.

Với `num_frames = 12`:

| | Cửa sổ chứa | Trải trên video |
|---|---|---|
| Anchor (`sliding_window`) | 12 frame, bước 4 | **45 frame** |
| Query (`soft_lock_buffer`) | 12 frame, bước 1 | **12 frame** |

⇒ Hai vector được đem so cosine với nhau nhưng **mô tả hai khoảng thời gian dài ngắn khác hẳn nhau**.
Đây là lỗi thật, độc lập với tranh luận về BatchNorm.

### 1.2. ⚠️ Bản sửa ĐẦU TIÊN của tôi KHÔNG có tác dụng

Lần đầu tôi sửa bằng cách đổi `SlidingWindowBuffer(self.num_frames, stride=1)` → `stride=self.stride`
cho `soft_lock_buffer`. **Sai.** Kiểm tra lại toàn repo:

```
infer.py:330                      self.sliding_window.should_extract()
evaluate_reid_robustness.py:295   self.sliding_window.should_extract()
phan_rang/infer_realworld.py:234  self.sliding_window.should_extract()
```

**`should_extract()` CHƯA BAO GIỜ được gọi trên `soft_lock_buffer`.** Ở T2_SEARCH, code chạy backbone
rồi `add()` **mỗi frame**, không hỏi stride:

```python
crop = crop_and_pad(frame, bbox, self.bbox_padding)
if crop is not None:
    ...
    feat_2560 = extract_cnn_feature(self.model, tensor_frame)   # chạy MỖI frame
    if len(self.soft_lock_buffer.features) > 0:
        self.soft_lock_buffer.add(feat_2560, sharpness)          # add MỖI frame
```

⇒ `soft_lock_buffer.stride` là **dead code**: gán nhưng không ai đọc. Hệ quả:

- Bug 1a **vẫn còn nguyên** sau bản sửa đầu
- Con số "cần 48 frame" tôi đưa ra cũng **sai** — thực tế soft lock vẫn chỉ cần `num_frames` frame
- Khớp với log cũ: `[101] 1/8` → `[108] 8/8` = 8 frame cho 8 feature → đúng stride 1

### 1.3. Bản sửa ĐÚNG (theo yêu cầu: soft lock liên tục, phần còn lại dùng stride)

| Thành phần | Thu thập | Bước thời gian đưa vào head |
|---|---|---|
| **SOFT LOCK** (`soft_lock_buffer`) | **Liên tục** (stride 1) → phản ứng nhanh | — |
| **HARD LOCK** | — | **Lấy cách quãng** `frame_stride` từ buffer trên |
| **Tracking / Memory Bank** (`sliding_window`) | Cách quãng sẵn qua `should_extract()` | 1 (đã cách quãng sẵn) |

Cơ chế: `soft_lock_buffer` thu liên tục nhưng chứa đủ `(num_frames−1)×stride + 1` frame; khi HARD LOCK
thì `features[::stride]` → ra **đúng `num_frames` mẫu, bước = `frame_stride`**.

**Vì sao `coarse score` không bị ảnh hưởng**: soft lock chỉ lọc thô bằng `visual` trên **từng frame
đơn lẻ** (`coarse_score(feat_2560)`), không dùng cửa sổ → không cần bước thời gian.

---

## 2. Đồng bộ `frame_stride` toàn pipeline

### 2.1. Ba khái niệm "stride" khác nhau đang bị lẫn

| Khái niệm | Ở đâu | Trước |
|---|---|---|
| Bước lấy mẫu khi **tạo dữ liệu** | `data_pipeline.py --frame-stride` | 4 (từ config) |
| Bước lấy mẫu khi **train/eval** | `_load_clip` | **`np.linspace` → ~5.45** ❌ |
| Bước lấy mẫu khi **infer** | `SlidingWindowBuffer` | anchor 2 (config), query **1** ❌ |

### 2.2. Vì sao bỏ `np.linspace` là đúng và **không cần chạy lại `data_pipeline.py`**

`np.linspace(0, len−1, num_frames)` lấy `num_frames` mốc **trải đều toàn danh sách**. Với
`num_before_frames = 16`, `num_frames = 12` → bước trung bình `(16−1)/(12−1) × 4 = 5.45`.

Cắt **contiguous** `frames[-12:]` cho ra **đúng dữ liệu mà `data_pipeline.py` sẽ sinh ra nếu chạy với
`num_before_frames = num_frames`** — tức **y hệt**, chỉ khác cách chọn. Nên **không cần sinh lại dữ liệu**.

Đã kiểm chứng bằng số:

```
before (take_last=True) : 116..160  n=12  bước={4}
after  (take_last=False): 500..544  n=12  bước={4}
sinh lại num_before=12  : 116..160
=> KHỚP HOÀN TOÀN

linspace (CŨ)  : 100..160  n=12  bước={4, 8}   (trung bình 5.45)
contiguous(MỚI): 116..160  n=12  bước={4}
```

### 2.3. `frame_stride` trở thành **nguồn duy nhất**

`infer.py::main()` đọc `data_pipeline.frame_stride` và ghi đè `infer.stride`, in cảnh báo nếu lệch.
Nên từ nay **không thể** để `infer.stride` trôi khỏi data/train.

---

## 3. Kiểm chứng

| Kiểm tra | Kết quả |
|---|---|
| `py_compile` 6 file (`infer`, `train_reid`, `evaluate_reid`, `calibrate_threshold`, `evaluate_reid_robustness`, `infer_realworld`) | ✅ pass |
| Còn `np.linspace` trong `_load_clip` nào không | ✅ không còn |
| Cả 6 chỗ `soft_lock_buffer = SlidingWindowBuffer(...)` | ✅ đều dùng sức chứa mới, `stride=1` |
| Parse YAML 4 config | ✅ `frame_stride = 4` ở cả 4; `infer.stride = 4` |
| Cắt contiguous == sinh lại data | ✅ khớp hoàn toàn |
| **Thiết kế soft→hard lock** (chạy code thật) | ✅ xem bảng dưới |
| **Encoder chịu được N nhỏ** (điều kiện để bỏ pad nhân bản) | ✅ N ∈ 1..64 đều finite, không NaN |

```
num_frames=12 stride=4  cap= 45  soft->hard: len=12 buoc=4  | tracking: len=12 buoc=4
num_frames= 8 stride=4  cap= 29  soft->hard: len= 8 buoc=4  | tracking: len= 8 buoc=4
num_frames= 6 stride=4  cap= 21  soft->hard: len= 6 buoc=4  | tracking: len= 6 buoc=4
num_frames=12 stride=2  cap= 23  soft->hard: len=12 buoc=2  | tracking: len=12 buoc=2
num_frames=12 stride=1  cap= 12  soft->hard: len=12 buoc=1  | tracking: len=12 buoc=1

N=1..4, 8, 12, 16, 45, 64 -> out (1,512) / seq (1,N,512), tất cả finite
```

---

## 4. ⚠️ Hệ quả cần biết: cửa sổ cần `(num_frames−1)×stride + 1` frame LIÊN TỤC

Vì `should_extract()` trả `True` ở lần gọi thứ 1, 5, 9, 13… (counter bắt đầu từ 0), để gom `k` feature
cần `(k−1)×stride + 1` frame:

| `num_frames` | stride 4 → cần | (trước đây soft lock chỉ cần `num_frames`) |
|---|---|---|
| 12 | **45** frame | 12 |
| 8 | **29** frame | 8 |
| 6 | **21** frame | 6 |

### 4.1. Đây **KHÔNG** phải vấn đề tốc độ xử lý

Cần phân biệt rõ hai thứ:

- **Tốc độ xử lý** (5× fps) quyết định **độ trễ** — 45 frame ≈ 9 s video ≈ 1.8 s wall-clock. **Không phải vấn đề.**
- **Số frame LIÊN TỤC mà target phải còn hiện diện** quyết định **có lock được hay không**. Đây mới là vấn đề.

Bằng chứng — `infer.py`, nhánh T2_SEARCH:

```python
elif self.state == self.T2_SEARCH:
    if is_absent or not valid_bbox:
        print(f"[{frame_idx}] UAV lost during T2_SEARCH. -> T1_LOST")
        self._transition_to_lost(frame_idx)      # ← XOÁ SẠCH buffer
        return
```

**Một frame target vắng mặt là reset toàn bộ cửa sổ về 0.** Nên nếu target tái xuất hiện 30 frame rồi
lại mất, dù xử lý 5× hay 500× fps, ta **chỉ gom được 30 feature** → không bao giờ đủ 45.

Log cũ chính là triệu chứng: `[100] reappeared` → `[101] 1/8` → … → `[109] UAV lost during T2_SEARCH`
— tái xuất chỉ **9 frame**.

### 4.2. Cùng ràng buộc này cũng có ở **offline**

`data_pipeline.py:154-155` **BỎ QUA** frame có bbox không hợp lệ:

```python
if bbox[2] <= 0 or bbox[3] <= 0:
    continue
```

`before_sampled` xin 16 mốc `t1, t1−4, … t1−60`, nhưng frame nào target chưa xuất hiện thì bị bỏ →
danh sách `frames` trong JSON **có thể ít hơn 16**.

**"Clip ngắn" = `len(frames) < num_frames`.** Với `stride = 4` và `num_frames = 12`, gallery cần target
hiện diện **~44 frame** trước `t1` mới đủ 12 mốc hợp lệ.

⇒ **Cả ba chặng đều có cùng ràng buộc ~45 frame:**

| Chặng | Cần target hiện diện liên tục |
|---|---|
| Gallery offline (train) | ~44 frame trước `t1` |
| Query offline (train) | ~44 frame sau `t2` |
| HARD LOCK lúc infer | **45 frame** |

**Cách đo trên Colab:**

```python
import json
for name in ("query", "gallery"):
    d = json.load(open(f"{processed_dir}/{name}.json"))
    L = [len(x["gallery_frames"] if name == "gallery" else x["query_frames"]) for x in d]
    print(name, "tổng", len(L), "| <12 frame:", sum(1 for n in L if n < 12),
          "| <6 frame:", sum(1 for n in L if n < 6), "| min", min(L))
```

---

## 5. Đính chính — các kết luận SAI của tôi trong quá trình làm

Ghi lại để không lặp lại:

| # | Tôi đã nói | Thực tế | Loại lỗi |
|---|---|---|---|
| 1 | "Train trải trên **cả event** (100+ frame)" | JSON chỉ chứa `num_before_frames` frame cách nhau `frame_stride` | Sai kiến thức về dữ liệu |
| 2 | "`BatchNorm1d` là thủ phạm" (`md/14thg9.md`) | `pre_bn` **kém hơn** `fused` về phân biệt (67.32% < 78.15%) → BN **đang giúp** | Viết kết luận **trước khi** đọc code |
| 3 | "Positional encoding (PE) là thủ phạm" | resnet N=16 (4 vị trí **chưa từng** nhận gradient) đạt 0.680 > N=12 (đã train) 0.349 | Giả thuyết bị số liệu bác bỏ |
| 4 | "z-norm sẽ nâng TAR **mạnh**" | Đo được **+0.22%** | Dự đoán sai |
| 5 | "Cần **48** frame" | **45** — công thức đúng là `(k−1)×stride + 1` | Sai số học |
| 6 | "Đã sửa xong bug 1a" | `soft_lock_buffer.stride` là **dead code** → bản sửa **không có tác dụng** | Báo hoàn thành khi chưa kiểm chứng |

**Bài học:** #2 và #6 cùng một dạng — **kết luận/báo cáo trước khi đọc hoặc chạy kiểm chứng**.
Từ nay với mọi thay đổi liên quan stride/buffer: **grep xem thuộc tính đó có thực sự được đọc không**
trước khi nói đã sửa.

---

## 6. Việc cần làm tiếp — phân tích 4 phương án

### 6.1. Bốn phương án **không cùng loại**

- **#1 Randomize N** là **chiến lược huấn luyện**.
- **#2 Bỏ clip ngắn / #3 Pad 0 / #4 Nhân bản** là **chính sách xử lý clip ngắn** — ba cách **loại trừ nhau**.
- **#1 bao trùm** #2/#3/#4: nếu N chọn ngẫu nhiên và **luôn chọn N ≤ số frame đang có**, clip 8 frame
  chỉ cần dùng N=8 → **không phải pad gì cả**.

### 6.2. Ưu / nhược

| # | Ưu | Nhược |
|---|---|---|
| **1. Randomize N** | ⭐ Sửa **đúng gốc rễ** (§13.2); ⭐ **giải luôn** bài toán clip ngắn; không mất mẫu; khớp thực tế infer (cửa sổ 8–30 frame) | **Phải train lại**; N phải chọn **theo batch** (nếu per-sample thì batch tensor lệch shape); mỗi độ dài nhận ít mẫu hơn; **không tự đảm bảo hiệu quả** nếu nguyên nhân là `SimpleS6Block` |
| **2. Bỏ clip ngắn** | Sạch dữ liệu, không frame giả, không bước = 0; cài đặt rẻ nhất | ⚠️ **Mất mẫu KHÔNG ngẫu nhiên** — clip ngắn = target hiện diện ngắn = **đúng ca khó nhất**; giảm event/danh tính → triplet khó hơn; không sửa được tính bất biến độ dài |
| **3. Pad 0** | Giữ đủ mẫu; vector 0 rõ ràng "không phải frame"; shape cố định | ⚠️ **Thiên vị có hệ thống**: `visual_plain = mean` theo thời gian → biên độ **bị kéo nhỏ theo tỉ lệ k/N**, sai lệch **tỉ lệ với độ dài clip**; model **chưa từng thấy** frame 0 → ngoài phân phối; consistency loss bị **thổi phồng** |
| **4. Nhân bản** (đang dùng) | Chi phí 0; giữ đủ mẫu; frame nhân bản **vẫn trong phân phối** → **không lệch biên độ** (khác #3) | **Bước thời gian = 0** → chuyển động giả "đứng yên"; model **không phân biệt được** frame giả; consistency loss bị **hạ thấp** (sim = 1 → loss = 0); vị trí nhân bản ở đuôi mang nghĩa khác nhau (before → sát `t1`; after → **xa `t2` nhất**) |

### 6.3. ⚠️ Điểm quyết định: checkpoint hiện tại

`train_reid.py`, `evaluate_reid.py`, `calibrate_threshold.py` dùng **cùng một `_load_clip`**. Nghĩa là:

> **Với checkpoint hiện tại, đổi chính sách pad sẽ làm số đo KHÔNG còn khớp với cách model đã được train.**

⇒ Nếu **không train lại**: **giữ #4, đừng đổi gì** — đổi chỉ làm con số eval/calibrate lệch khỏi hành vi thật.

### 6.4. Khuyến nghị

**Nếu train lại → #1 + #2 (lai), làm CHUNG một lần với `mamba_ssm` thật:**

1. Cho N bốc ngẫu nhiên trong `{6, 8, 12, 16}` (**theo batch**; gallery và query cùng một cặp dùng **cùng N**).
2. Với clip dài `L`: chọn N trong tập **sao cho N ≤ L** → clip 8 frame dùng N=8, **không pad**.
3. Chỉ **bỏ** clip có `L < 6` (dưới N nhỏ nhất) — số này rất ít.

Vì sao lai: **#1** gỡ gốc rễ và **tự động** làm #2/#3/#4 gần như không cần thiết; **#2** chỉ còn là lưới
an toàn cho vài clip cực ngắn → mất **rất ít** mẫu, tránh được nhược điểm lớn nhất của #2 thuần.
**Không dùng #3** (thiên vị biên độ theo độ dài là lỗi âm thầm, khó phát hiện). **Không dùng #4** khi
train lại (bước thời gian = 0 là thứ vừa mất công loại bỏ ở toàn pipeline).

**Làm `mamba_ssm` thật trong CÙNG lần train đó** — vì cả hai đều buộc phải train lại. §13.5 #3 nói rất
có thể phần lớn hiện tượng §13 là **artifact của `SimpleS6Block`**; nếu đúng thì randomize N một mình
chỉ chữa được nửa vấn đề, và sẽ mất thêm một chu kỳ train để phát hiện.

**Nếu chưa train lại → giữ #4, dồn công sức việc khác** (đo tỉ lệ clip ngắn, chuẩn bị `mamba_ssm`).

---

## 7. Điểm còn tồn

| # | Điểm còn tồn | Ảnh hưởng |
|---|---|---|
| 1 | **Chưa đo tỉ lệ clip ngắn** (`len(frames) < 12` và `< 6`) trên dữ liệu thật | Không biết phương án #2 đắt hay rẻ (1–2% thì gần như miễn phí; 20%+ thì phải cân nhắc lại) |
| 2 | **Chưa chạy `infer.py` thật** sau khi đồng bộ stride | Toàn bộ kiểm chứng mới ở mức `py_compile` + test đơn vị trên buffer/encoder. Chưa có log Colab |
| 3 | **Ngưỡng `reid_threshold = 0.75`** vs calibrated 0.852 | Pipeline vẫn chạy ở điểm làm việc khác điểm đã calibrate |
| 4 | **Ngưỡng calibrated KHÔNG chuyển được sang cổng T2_SEARCH** | `visual` plain-mean có `t* = 0.9956`, **cao hơn** dải `visual_plain` thật của pipeline (0.986–0.994) → cần dương/âm **trong chuỗi** |
| 5 | Các lần chạy Jetson / robustness / local cũ | **Phải chạy lại** vì `num_frames` và `frame_stride` vừa đổi — kết quả cũ không còn so sánh được |
| 6 | Giao thức eval **xuyên chuỗi** (`md/15thg9.md` §12.1) | Vẫn là vấn đề lớn nhất chưa xử lý: 340 identity ↔ 340 sequence **song ánh**, **0% impostor cùng chuỗi**. Mọi metric hiện tại đo một bài toán khác với bài toán cổng T2_SEARCH |

---

## 8. Rà soát độc lập (do agent thực hiện, 22/9) — đọc xong `22thg9.md`

### 8.1. Đã xác nhận trong code (không chỉ tin báo cáo)

| # | Điều bạn báo | Kiểm chứng |
|---|---|---|
| 1 | Bỏ `np.linspace` trong `_load_clip` | ✅ `train_reid.py:171-183` + `evaluate_reid.py:64` + `calibrate_threshold.py:98`. `linspace` còn lại chỉ ở chỗ vẽ plot, không liên quan |
| 2 | `soft_lock_buffer.stride` là dead code | ✅ `should_extract()` chỉ được gọi **1 chỗ**: `infer.py:346` (`self.sliding_window`). Không có chỗ nào gọi trên `soft_lock_buffer` |
| 3 | Phân vai stride | ✅ `infer.py:269` `SlidingWindowBuffer(self.soft_lock_capacity, stride=1)`; `:75` `self.features[::stride]`; `:474` `features[::self.stride]` |
| 4 | `frame_stride` là nguồn duy nhất | ✅ `configs/config_colab.yaml:32` `data_pipeline.frame_stride: 4`, `:112`/`:141` `stride: 4`; `infer.py` đọc và ghi đè |
| 5 | Công thức `(num_frames−1)×stride+1` | ✅ 12→45, 8→29, 6→21 — số học đúng; `[::4]` trên 45 phần tử cho đúng 12 mẫu (44 = 11×4) |
| 6 | `take_last`: before lấy cuối, after lấy đầu | ✅ `train_reid.py:182` `frames[-n:] if take_last else frames[:n]` |

**Thiết kế khớp train↔infer rất tốt** — tôi kiểm tra và xác nhận:
- Gallery (train) = `frames[-12:]` = `t1−44 … t1`; HARD LOCK = buffer 45 frame liên tục kết thúc ở frame
  hiện tại, `[::4]` → cũng kết thúc ở frame cuối. **Khớp mốc neo (t1).**
- Query (train) = `frames[:12]` = `t2 … t2+44`; HARD LOCK bắt đầu thu từ frame tái xuất t2 và đầy ở
  `t2+44` → **khớp mốc neo (t2).**

### 8.2. ⚠️ LỖI MỚI tìm được: bất đối xứng guard ở nhánh **Last-moment update**

**Có ở cả 3 file** (`infer.py:325`, `evaluate_reid_robustness.py:291`, `phan_rang/infer_realworld.py:284`):

| Nhánh | Guard | N đưa vào `temporal_encoder` |
|---|---|---|
| Tracking (thêm anchor/recent định kỳ) | `if self.sliding_window.is_ready() and time_elapsed >= ...` (`infer.py:357`) | **đúng `num_frames`** ✅ |
| **Last-moment update before LOST** | `if len(self.sliding_window.features) > 0:` (`infer.py:325`) | **1 … `num_frames`** ❌ |

Comment ngay trên đó biện minh:
```python
# (temporal_encoder dùng pos_embed[:, :N, :] và conv1d cắt về L nên N nhỏ OK)
```

**Chính giả thuyết này đã bị `md/15thg9.md` §13 bác bỏ bằng số liệu**: resnet `temporal` = 0.977 @N=8
nhưng **0.349 @N=12**. Encoder **KHÔNG** "N nhỏ OK".

**Vì sao nghiêm trọng:** last-moment update tạo ra entry **khớp nhất với gallery lúc train**
(`take_last=True` = frame sát `t1`) — tức entry **quan trọng nhất** cho HARD LOCK. Nếu lúc mất dấu cửa
sổ chưa đủ `num_frames`, token temporal của entry đó được tính ở N sai → theo §13 có thể lệch nặng →
**entry bị đóng băng luôn** (`add_anchor` không bao giờ đẩy phần tử cũ ra).

**Hướng sửa:**
- **Trước khi train lại**: đừng tính ở N thiếu. Dùng `is_ready()`; nếu chưa đủ thì **vẫn lưu entry nhưng
  đánh dấu** `temporal_valid=False`, và để cổng HARD LOCK chỉ so `visual` (nhánh **miễn nhiễm** với N:
  0.986–0.994 ở mọi N). Hoặc đơn giản: bỏ entry đó.
- **Sau khi randomize N**: tính ở N thiếu trở thành **hợp lệ** — khi đó giữ nguyên.

### 8.3. ⚠️ Cảnh báo kỳ vọng: bản sửa stride sẽ làm **SỐ LOCK GIẢM MẠNH**

Đây là điều dễ bị đọc sai thành "sửa hỏng". Yêu cầu mới là `(num_frames−1)×stride+1` = **45 frame LIÊN
TỤC** (với `num_frames=12, stride=4`), so với trước là 12. Mà `is_absent` một frame là **reset về 0**.

Số liệu từ `md/test.txt`: lần tái xuất ở frame 100 chỉ kéo dài **10 frame** (100→109) → với yêu cầu 45
frame thì **event đó vĩnh viễn không thể lock**. Số chu kỳ có được quyết định trước đây (ở 8/12/16 frame
liên tiếp) đã chỉ là 6/4/3 trên 8; nay cần 45 → **gần như 0/8**.

⇒ **Không được đánh giá bản sửa này bằng số lock.** Nó đánh đổi *độ chính xác mỗi lần quyết định* lấy
*số cơ hội quyết định*. Muốn lấy lại số cơ hội thì **bắt buộc phải có N thích ứng** ("dùng N lớn nhất ≤
số frame đang có") — mà điều đó **chỉ an toàn sau khi randomize N + train lại**, vì hiện tại model sụp ở
N sai (§13). Nói cách khác: **bản sửa stride biến `randomize N` từ "nên làm" thành "BẮT BUỘC".**

### 8.4. Hai điều chỉnh nhỏ

1. **§7 #4 cần đo lại**: dải `visual_plain` 0.986–0.994 lấy từ `md/test.txt` — chạy với stride **lệch**
   (anchor 2 / query 1). Sau khi đồng bộ stride 4, dải này **sẽ đổi**. Đừng so ngưỡng `visual`
   `t* = 0.9956` với số cũ.
2. **§5 #1 của bạn đúng** — và tôi đã sửa lại `md/15thg9.md` §13.3(a) (bản cũ của tôi viết "100+ frame",
   sai). Kèm theo đó: vì lệch bước thời gian thật chỉ **5.45 vs 4** (và anchor 2 vs query 1), nên
   **cơ chế (a) yếu hơn tôi đã trình bày** → càng củng cố kết luận ở §6.4: **đồng bộ stride KHÔNG chữa
   được độ nhạy theo N**, phải làm `mamba_ssm` thật + randomize N.

### 8.5. Đánh giá §6.4 (khuyến nghị #1 + #2 + mamba_ssm thật)

Đồng ý, và bổ sung 2 lý do cụ thể hơn:
- **#3 Pad 0 nguy hiểm hơn cả nhận định của bạn**: `visual_plain = mean` theo thời gian, nên tỉ lệ
  `k/N` frame 0 làm **biên độ co theo độ dài clip** → lệch **tương quan với chính biến N đang muốn
  randomize** ⇒ hai nguồn lệch **không tách được** khi debug. Tránh là đúng.
- **#1 phải chọn N theo BATCH** (bạn đã ghi): thêm ràng buộc **gallery và query của cùng một cặp phải
  dùng CÙNG N**, nếu không phép so cosine trong `HardTripletLoss` là giữa hai không gian khác nhau.

---

## 9. ĐÃ TRIỂN KHAI: nới reset trong T2_SEARCH (`gap_tolerance`) — phương án C

### 9.1. Thay đổi

| File | Thay đổi |
|---|---|
| `infer.py` | `self.gap_tolerance = cfg.get('t2_search_gap_tolerance', 2)` + `self._absent_streak = 0`; nhánh T2_SEARCH **không reset ngay** khi vắng, mà đếm `_absent_streak` |
| `evaluate_reid_robustness.py` | Y hệt |
| `phan_rang/infer_realworld.py` | Y hệt, nhưng trigger là **track `soft_lock_id` mất khỏi frame** (pipeline không dùng GT). Bỏ việc clear soft-lock trong vòng cleanup `lost_tids` để nhánh `else` quyết định (vẫn xoá khỏi `candidate_scores` để track quay lại được **chấm lại** coarse) |
| 4 config | Thêm `infer.t2_search_gap_tolerance: 2` (+ `infer_robustness`, `infer_realworld`) |

Cơ chế: `_absent_streak += 1`; nếu `<= gap_tolerance` → **in log và `return`**, GIỮ nguyên
`soft_lock_buffer` + feature đã thu; chỉ khi vượt ngưỡng mới reset thật và sang `T1_LOST`.
Frame hiện diện lại → `_absent_streak = 0`.

> `configs/config_h100.yaml` không đụng. `configs/config_jetson_smoke.yaml` **không có section
> `infer`** nên không cần thêm — code dùng default `2`.

### 9.2. Kiểm chứng — mô phỏng máy trạng thái (logic y hệt đã chèn)

`num_frames=12, stride=4` → cần 45 feature:

| Kịch bản | `tol=0` (hành vi cũ) | `tol=2` | `tol=5` |
|---|---|---|---|
| 45P liên tục | lock | ✅ lock | ✅ lock |
| **30P + 1A + 15P** (dropout 1 frame) | ❌ buffer 15, **không lock** | ✅ **buffer 45, lock@45** | ✅ lock |
| 20P + 3A + 25P (dropout 3 frame) | ❌ | ❌ buffer 25 | ✅ buffer 45, lock@47 |
| **10P + 20A** (đúng ca trong `md/test.txt`) | ❌ | ❌ **vẫn không lock** | ❌ |

### 9.3. ⚠️ GIỚI HẠN QUAN TRỌNG — C **KHÔNG** chữa được ca đã ghi trong log

Mô phỏng cho thấy `gap_tolerance` chỉ chữa **detection dropout ngắn**. Ca thật trong `md/test.txt`
(frame 100→108 hiện diện **10 frame** rồi vắng hẳn) **vẫn không lock được**, vì 10 < 45.

| | Cần target hiện diện | Ca log có |
|---|---|---|
| stride 4, N=12 (hiện tại) | **45 frame** | 10 → **thiếu 4.5×** |
| stride 4, N=8 | 29 frame | 10 → thiếu 2.9× |
| **stride 1, N=12** | **12 frame** | 10 → **thiếu 1.2×** |
| stride 1, N=8 | 8 frame | 10 → **ĐỦ** ✅ |

⇒ **Nếu vấn đề thật là "cửa sổ tái xuất ngắn", C là chưa đủ — phải giảm yêu cầu frame**, tức
phương án A (stride 1) hoặc N nhỏ hơn. C chỉ nên coi là **lưới an toàn cho dropout**, không phải
giải pháp cho cửa sổ ngắn.

### 9.4. Việc cần đo để chọn `gap_tolerance`

Chưa biết phân bố "số frame hiện diện liên tục sau khi tái xuất" trên dữ liệu thật. Cần:

```python
# Đếm chuỗi frame hiện diện liên tục dài nhất sau mỗi lần tái xuất
import json
d = json.load(open(f"{processed_dir}/query_test.json"))
for x in d:
    print(x["sequence_id"], x["event_index"], x["disappearance_duration_frames"],
          len(x["query_frames"]))
```

Nếu phần lớn < 12 frame → **phải làm A**, `gap_tolerance` chỉ là phụ. Nếu phần lớn ≥ 45 frame →
C là đủ và `gap_tolerance = 2` hợp lý.

---

## 10. ✅ ĐÃ TRIỂN KHAI ĐÚNG THIẾT KẾ: HAI CỬA SỔ SONG SONG (22/9 lần 2)

### 10.1. Thiết kế chốt (theo yêu cầu)

| # | Thành phần | Số mẫu | Bước | Cần bao nhiêu frame video | Vai trò |
|---|---|---|---|---|---|
| 1 | **Memory Bank** (`sliding_window`, anchor/recent) | N | **`frame_stride` = 4** | 45 | Tham chiếu để so cosine |
| 2 | **SOFT LOCK** (`soft_lock_buffer`) | N | **1** (liền nhau) | **N = 12** | **NHANH** — chọn ứng viên / xem điểm. **KHÔNG quyết định** |
| 3 | **HARD LOCK** (`hard_lock_buffer`) | N | **`frame_stride` = 4** | 45 | **CHÍNH XÁC** — cửa sổ đem so bank, mới được chốt |

**Ý tưởng gốc (làm rõ 22/9):** soft/hard lock sinh ra cho tình huống **sau khi tái xuất có NHIỀU mục
tiêu** — mục tiêu nào có điểm coarse cao nhất sẽ được đem đi HARD LOCK để chốt. Soft lock là bước
**chọn ứng viên**, nên nó phải nhanh và chưa cần chính xác. Trong data hiện tại nhãn track chỉ theo
**1 mục tiêu**, nên soft lock **chưa có tác dụng thực chất** — nó chỉ để xem điểm. Vì vậy soft lock
**không được phép chặn** đường đi của HARD LOCK.

### 10.2. Vì sao phải là HAI buffer, không phải một

Không thể lấy N mẫu bước 4 từ N frame liền nhau (12 frame chỉ cho `[::4]` = 3 mẫu → N=3, và lệch với
bank N=12 — đúng bug 1a). Nên hai cửa sổ **thu song song, độc lập**:

```python
self.soft_lock_buffer = SlidingWindowBuffer(self.num_frames, stride=1)          # N frame liền
self.hard_lock_buffer = SlidingWindowBuffer(self.num_frames, self.stride)      # N mẫu cách 4
...
# trong T2_SEARCH, mỗi frame:
self.soft_lock_buffer.add(feat, sharp)                    # liên tục
if self.hard_lock_buffer.should_extract():                # mỗi 4 frame
    self.hard_lock_buffer.add(feat, sharp)
if _new_hard_sample and self.hard_lock_buffer.is_ready():  # đủ N mẫu -> MỚI chạy lọc tinh
    bundle = compute_fused_vector(self.model, self.hard_lock_buffer)   # stride=1, đã cách quãng sẵn
```

### 10.3. Kiểm chứng — mô phỏng đúng logic `SlidingWindowBuffer`

```
num_frames=12, frame_stride=4
  SOFT LOCK ready ở frame thứ  12   (12 frame liên tục)
  HARD LOCK ready ở frame thứ  45   (12 mẫu, khoảng cách [4,4,4,4,4,4,4,4,4,4,4])
  soft_lock_capacity = 12  (trước đây 45)
```

| | Trước (1 buffer 45, `[::4]`) | Sau (2 buffer) |
|---|---|---|
| Soft lock sẵn sàng | frame 45 | **frame 12** (nhanh hơn 3.75×) |
| Hard lock sẵn sàng | frame 45 | frame 45 (không đổi — ràng buộc số học) |
| Cửa sổ HARD LOCK | `soft[::4]` (thủ công) | `hard_lock_buffer` (tự nhiên) |
| Rolling window sau FAILED | `pop(0)` × stride (thủ công) | **tự động** — mỗi 4 frame nhận 1 mẫu mới, đẩy mẫu cũ nhất |
| `sliding_window` sau HARD LOCK | nạp từ `soft[::4]` | nạp từ `hard_lock_buffer.features` |

### 10.4. Đã sửa ở đâu

| File | Thay đổi |
|---|---|
| `infer.py` | `soft_lock_buffer = SlidingWindowBuffer(num_frames, stride=1)`; **thêm** `hard_lock_buffer = SlidingWindowBuffer(num_frames, stride)`; `soft_lock_capacity = num_frames`, `hard_lock_capacity = (num_frames-1)*stride+1`; `_soft_lock_announced`; lọc tinh đọc `hard_lock_buffer`; bỏ `pop(0)` thủ công; `sliding_window` nạp từ `hard_lock_buffer`; `draw_ui` hiện `soft x/N \| hard y/N` |
| `evaluate_reid_robustness.py` | Y hệt. Thu `hard_lock_buffer` **vô điều kiện** (không phụ thuộc coarse) để cửa sổ không vỡ khi coarse dao động |
| `phan_rang/infer_realworld.py` | Y hệt, theo `soft_lock_id` (không dùng GT) |

Đã `py_compile` cả 3 file ✅. Đã grep xác nhận: không còn `soft_lock_buffer.features[::stride]` nào;
cả 3 chỗ lọc tinh (`infer.py:484`, `evaluate_reid_robustness.py:403`,
`phan_rang/infer_realworld.py:384`) đều dùng `hard_lock_buffer`.

### 10.5. ⚠️ Điều KHÔNG đổi — phải nhớ

**HARD LOCK vẫn cần 45 frame target hiện diện** (12 mẫu × bước 4). Với ca trong `md/test.txt` (tái xuất
10 frame), HARD LOCK **vẫn không thể chốt**. Hai cửa sổ này **không** làm hard lock nhanh hơn — chúng
làm **soft lock nhanh hơn** (45 → 12) và tách bạch vai trò. Nếu muốn hard lock chốt được trong cửa sổ
ngắn thì vẫn phải giảm yêu cầu: `frame_stride = 1` (→ 12 frame) hoặc `num_frames` nhỏ hơn.

`gap_tolerance` (§9) vẫn giữ nguyên tác dụng: chịu được dropout ngắn cho **cả hai** buffer.

---

## 11. ⚠️ LỖI LOGIC do hai cửa sổ song song — và cách sửa

### 11.1. Vấn đề

Hai cửa sổ song song chỉ đúng **nếu cửa sổ HARD LOCK thuộc ĐÚNG ứng viên mà SOFT LOCK đã chọn**.
Nếu ứng viên đổi giữa chừng mà cửa sổ hard lock không bị xoá, chuỗi temporal sẽ **trộn frame của hai
vật khác nhau** → điểm hard lock **không còn là "điểm của thằng có soft lock cao nhất"**, mà là điểm
của một chuỗi lai vô nghĩa.

Code lần đầu của tôi **có lỗi này**: ở `phan_rang/infer_realworld.py`, chỗ `soft_lock_id != best_tid`
chỉ xoá `soft_lock_buffer`, **không** xoá `hard_lock_buffer`.

### 11.2. Quy tắc đúng

> Cửa sổ HARD LOCK thuộc một ứng viên. Reset nó khi **mất ứng viên thật** (T1_LOST) hoặc **ứng viên đổi**.

| Sự kiện | `soft_lock_buffer` | `hard_lock_buffer` | Lý do |
|---|---|---|---|
| T1_LOST | clear | **clear** | mất ứng viên |
| Vào T2_SEARCH | clear | **clear** | bắt đầu lại |
| **Ứng viên đổi** (`soft_lock_id != best_tid`) | clear | **clear** ✅ đã sửa | cửa sổ phải thuộc ứng viên mới |
| HARD LOCK thành công | clear | **clear** | nạp xong vào `sliding_window` |
| Coarse tụt **1 frame** (nhánh `else`) | clear | **KHÔNG clear** (cố ý) | hard lock thu vô điều kiện; 1 frame tụt không được phá cửa sổ 45 frame |

### 11.3. Vì sao `infer.py` / `evaluate_reid_robustness.py` **không** bị lỗi này

Ở hai pipeline đó `bbox` lấy từ **nhãn GT** → mỗi frame chỉ có **MỘT** ứng viên, và ứng viên đó **tồn
tại ngay từ frame tái xuất**. Nên "ứng viên soft lock chọn" luôn là target, không bao giờ đổi → cửa sổ
hard lock luôn thuộc target → đúng. Đây là **may**, không phải thiết kế.

Đã thêm ghi chú `⚠️ RÀNG BUỘC ỨNG VIÊN` vào cả hai file: **nếu mở rộng sang nhiều mục tiêu thì BẮT BUỘC
thêm `hard_lock_buffer.clear()` mỗi khi ứng viên đổi.**

### 11.4. Hệ quả về mặt thời gian (phải biết)

Khi ứng viên đổi, cửa sổ hard lock reset → **đồng hồ 45 frame đếm lại từ đầu**. Vậy tổng thời gian từ
lúc tái xuất đến khi hard lock chốt được là:

```
t_hard_lock  =  t_ứng_viên_ổn_định  +  (num_frames − 1) × frame_stride  +  1
                    ↑                         ↑
          lúc soft lock chọn xong        = 45 frame (N=12, stride=4)
```

Với data 1 mục tiêu, `t_ứng_viên_ổn_định` = 0 (ứng viên có ngay) → 45 frame như cũ.
Với nhiều mục tiêu, hard lock **không thể** chốt sớm hơn `N_soft + 45` frame. Nếu muốn nhanh hơn thì
phải giảm `frame_stride` — điều này **không** tránh được bằng cách xếp hai cửa sổ song song.

---

## 12. ✅ PHƯƠNG ÁN B — SOFT LOCK LÀ CỔNG CHẶN, HARD LOCK LÀ QUYẾT ĐỊNH

### 12.1. Thiết kế chốt

> **Soft lock KHÔNG quyết định danh tính.** Nó chỉ **tính điểm**. Điểm ≥ ngưỡng → mới cho phép tính
> HARD LOCK. Điểm < ngưỡng → **quay lại `T1_LOST`** và tính soft lock lại từ đầu. **HARD LOCK mới là
> quyết định.**

```
T1_LOST ──(target hiện diện)──> T2_SEARCH
   ↑                                 │
   │                          thu N frame LIÊN TỤC
   │                                 ↓
   │                        ┌─── điểm SOFT LOCK ───┐
   │                        │  coarse(mean N frame)│
   │                        └──────────┬───────────┘
   │                          < ngưỡng │ ≥ ngưỡng
   └───────────────────────────────────┘         │
                                                 ↓
                                    (cửa sổ HARD LOCK đã thu SONG SONG)
                                                 ↓
                                    đủ N mẫu stride 4 → Lọc Tinh → HARD LOCK
```

### 12.2. Điểm soft lock lấy từ đâu

`coarse_score` = **cosine visual-only** (`_max_sim(feat, "visual")`), nên:

- **Không dính lỗi temporal theo N** — đã đo `visual_plain` = 0.986–0.994 ở **mọi** N (§13 `15thg9.md`).
  Đây là lý do chọn coarse thay vì fused: fused sụp ở `N = N_train` (0.318 / 0.157), coarse thì không.
- **Trung bình `num_frames` frame** → chống nhiễu per-frame. Một frame nhiễu không phá được cổng.

```python
mean_visual = torch.stack(list(self.soft_lock_buffer.features)).mean(dim=0)
soft_score  = self.memory_bank.coarse_score(mean_visual)
```

### 12.3. Quan hệ thời gian — ⚠️ MỤC NÀY ĐÃ SAI, XEM ĐÍNH CHÍNH Ở §13

> Bản đầu tôi cho hard lock **thu song song** và viết `max(12,45) = 45`. **SAI PIPELINE** —
> xem §13. Bản đúng là **thu tuần tự**: `t_hard_lock ≈ 12 + 44 = 56` frame.

Cửa sổ HARD LOCK **thu song song từ lúc tái xuất** (chỉ là feature đã có sẵn, rẻ), nhưng **chỉ được
TÍNH** khi soft lock đã PASS. Nên:

```
t_hard_lock = max( t_soft_fill , t_hard_fill ) = max( N , (N−1)×stride+1 ) = 45 frame
```

Nếu thu **tuần tự** (đợi soft pass rồi mới bắt đầu thu hard) thì sẽ là **12 + 45 = 57 frame** — chậm
hơn 12 frame mà không đổi ngữ nghĩa. Tôi chọn **song song**; nếu bạn muốn tuần tự thì nói, sửa 1 dòng.

### 12.4. Kiểm chứng — mô phỏng luồng đầy đủ

| Kịch bản | Kết quả mô phỏng |
|---|---|
| Target hiện 80 frame, coarse 0.9 | `frame 12: SOFT PASS` → `frame 45: HARD LOCK`, 12 mẫu `[1,5,…,45]` ✅ |
| Coarse thấp ở frame 0–23, cao từ 24 | `frame 12: SOFT FAIL` → `frame 13: T2_SEARCH` → `frame 25: FAIL` → `frame 26: T2_SEARCH` → `frame 38: SOFT PASS` → `frame 71: HARD LOCK` ✅ |
| Target chỉ hiện **10 frame** (ca `md/test.txt`) | ❌ **không chốt được** — soft lock cần 12 frame |

**Không kẹt:** `T1_LOST → T2_SEARCH` chỉ cần điều kiện *"target đang hiện diện"* (`if not is_absent and
valid_bbox`), **không** đòi cạnh absent→present. Nên soft fail xong thì frame kế tiếp vào lại ngay,
thử lại với cửa sổ mới. Đã mô phỏng 3 lần thử liên tiếp → OK.

### 12.5. Đã sửa ở đâu

| File | Thay đổi |
|---|---|
| `infer.py` | `_soft_lock_passed` (reset ở 4 chỗ: init, `_transition_to_lost`, vào T2_SEARCH, HARD LOCK xong); điểm soft = `coarse(mean N frame)`; PASS → `_soft_lock_passed=True`, FAIL → `_transition_to_lost()` + `return`; hard check gate bằng `self._soft_lock_passed and _new_hard_sample and …is_ready()` |
| `evaluate_reid_robustness.py` | Y hệt. Thu soft lock **vô điều kiện** một khi đã bắt đầu (cổng thật là điểm cả cửa sổ, không phải coarse từng frame); nhánh `else` coarse-tụt giờ không làm gì |
| `phan_rang/infer_realworld.py` | Y hệt (5 chỗ reset, thêm chỗ **đổi ứng viên** `soft_lock_id != best_tid`) |

`py_compile` cả 3 file ✅.

### 12.6. ⚠️ Ngưỡng `soft_lock_threshold` cần hiệu chỉnh lại

Config hiện tại: `configs/config_colab.yaml:122` đặt `soft_lock_threshold: 0.3`, còn default trong code
là `0.50`. Nhưng điểm coarse của target đo được là **~0.99** → ngưỡng `0.3` **gần như luôn PASS**, tức
cổng chặn gần như **không có tác dụng**. Muốn cổng có tác dụng thật thì phải **hiệu chỉnh lại ngưỡng**
theo phân bố điểm coarse thật (dùng `calibrate_threshold.py`), nếu không thì phương án B chỉ đẹp trên
giấy.

---

## 13. ⚠️ ĐÍNH CHÍNH §12.3 — HARD LOCK PHẢI THU **TUẦN TỰ**, KHÔNG SONG SONG

### 13.1. Lỗi của tôi

Bản §12.3 tôi cho cửa sổ HARD LOCK **thu song song từ lúc tái xuất** rồi chỉ chặn khâu *tính*, và lý
luận "thu song song rẻ vì feature đã có sẵn". **Lý luận đó sai về pipeline:**

> Khi **chưa có điểm soft lock** thì **chưa biết phải thu frame của MỤC TIÊU NÀO**.

Trong cảnh nhiều UAV, cửa sổ thu song song sẽ chứa frame của **một vật bất kỳ** — có thể không phải
vật mà soft lock sau đó chọn. Điểm hard lock khi đó là điểm của **sai vật**. Ở data hiện tại chỉ có 1
mục tiêu nên nó "tình cờ đúng", nhưng **đó không phải lý do để pipeline sai**.

(Ngoại lệ duy nhất làm song song đúng là giữ **feature history riêng cho từng track** rồi lấy hồi tố —
tôi **không** implement cái đó, nên bản song song là sai thật.)

### 13.2. Đã sửa

Cửa sổ HARD LOCK **chỉ bắt đầu thu sau khi soft lock PASS**:

```python
_new_hard_sample = False
if self._soft_lock_passed:                       # ← chỉ thu SAU khi biết mục tiêu nào
    _new_hard_sample = self.hard_lock_buffer.should_extract()
    if _new_hard_sample:
        self.hard_lock_buffer.add(feat_2560, sharpness)
if self._soft_lock_passed and _new_hard_sample and self.hard_lock_buffer.is_ready():
    ...   # Lọc Tinh -> HARD LOCK
```

Đã sửa ở cả 3 file: `infer.py`, `evaluate_reid_robustness.py`, `phan_rang/infer_realworld.py`.
Đã grep xác nhận `should_extract()` nằm trong `if self._soft_lock_passed:` ở **cả 3** file.

### 13.3. Hệ quả thời gian (số đúng)

```
t_soft_lock  = N                              = 12 frame   (N frame liên tục)
t_hard_lock  = N + (N−1)×frame_stride          = 12 + 44 = 56 frame
```

| | Song song (§12.3, SAI) | Tuần tự (§13, ĐÚNG) |
|---|---|---|
| Hard lock chốt ở frame | 45 | **56** |
| Chậm hơn | — | **+11 frame** |
| Đúng khi nhiều mục tiêu | ❌ | ✅ |

### 13.4. Kiểm chứng — mô phỏng lại luồng tuần tự

| Kịch bản | Kết quả mô phỏng |
|---|---|
| Target hiện 120 frame, coarse 0.9 | `frame 12: SOFT PASS` → `frame 56: HARD LOCK`, mẫu `[12,16,…,56]` ✅ |
| Coarse thấp 0–11 (soft FAIL) | `12: FAIL` → `13: T2_SEARCH` → `25: SOFT PASS` → `69: HARD LOCK` ✅ (thử lại được) |
| Target chỉ hiện **10 frame** | ❌ không chốt — soft lock cần 12 frame |

### 13.5. Hệ quả cho ca trong `md/test.txt`

Ca đã ghi (tái xuất **10 frame**) giờ còn xa hơn: cần **12 frame** cho soft lock, rồi **44 frame nữa**
cho hard lock. Với `num_frames=12, frame_stride=4` thì **không có cách nào** chốt trong 10 frame.

| Cấu hình | Frame cần để HARD LOCK |
|---|---|
| N=12, stride=4 (hiện tại) | **56** |
| N=8, stride=4 | 8 + 28 = **36** |
| N=12, stride=1 | 12 + 11 = **23** |
| **N=8, stride=1** | 8 + 7 = **15** |

⇒ Nếu mục tiêu là chốt được trong cửa sổ tái xuất ngắn thì **vẫn phải giảm `frame_stride` hoặc N**.
Pipeline giờ đã đúng; nhưng đúng ≠ chốt kịp. Đây là hai việc khác nhau.

---

## 14. Log gây hiểu nhầm + fix, và §15. `SimpleS6Block` viết lại

### 14.1. Log cũ trông như hard lock không chạy — thực ra nó CHẠY

Từ log thật (`UAV-Anti-UAV_Test_000059`, dinov3, N=12):

| Lần thử | Reappear | Soft pass | Hard lock | Kết quả |
|---|---|---|---|---|
| 1 | 100 | — | — | ❌ target hiện **9 frame** (100–108) → soft chưa đủ 12 |
| 2 | 122 | 134 | 178 (0.749 FAIL) → **182 (0.771 PASS)** | ✅ latency 60 |
| 3 | 214 | 226 | — | ❌ mất target ở 246 |
| 4 | 250 | — | — | ❌ target hiện **10 frame** |
| 5 | 533 | 545 | **589 (0.865 PASS)** | ✅ latency 56 |
| 6 | 670 | 682 | — | ❌ mất target ở 701 |

`178 − 134 = 44` và `589 − 545 = 44` = `(12−1)×4` ✅. Rolling window `182 − 178 = 4` ✅.
**Pipeline đúng** — chỉ có log in `Soft Lock collecting: 12/12` lặp 44 frame vì soft lock vẫn thu sau khi đã có điểm.

**Đã sửa:** soft lock **dừng thu** ngay khi có điểm (`if self._soft_lock_announced: pass`), và thêm log
`HARD LOCK collecting: k/12 (moi 4 frame)` để nói đúng việc đang làm.

**3 điều log này nói ra:**
1. **Soft lock (12 frame) giờ là nút thắt ĐẦU TIÊN**: 3/6 lần thử chết ở soft. Có **hai** ngưỡng chết: 12 (soft) và 56 (hard).
2. `soft_lock_threshold = 0.3` **hoàn toàn vô hiệu** — điểm soft đo được 0.990–0.996. Cổng chưa bao giờ chặn.
3. Kết quả **tái hiện chính xác** vùng sụp đã đo: dinov3 train N=8, infer N=12 → `fused 0.749/0.771`,
   `temporal 0.937–0.947`. So bảng cũ `md/test.txt`: dinov3 N=12 → `fused 0.747`, `temporal 0.939`. **Trùng khớp.**

---

## 15. `SimpleS6Block`: bỏ công thức O(L²), dùng recurrence tuần tự

### 15.1. Phát hiện

`mamba-ssm` **không cài được trên Colab T4** (build wheel fail ở bước requirements — cần `nvcc` +
CUDA khớp với torch). **Nhưng không cần**, vì fallback tự nó có vấn đề hiệu năng:

```python
P = torch.cumsum(W_t, dim=-1)                       # tích luỹ theo L
M = P.unsqueeze(-1) - P.unsqueeze(-2)               # [B, d_inner, d_state, L, L]  ← O(L²)
weights = torch.exp(M)
h_t = (weights * V_t.unsqueeze(-2)).sum(dim=-1)
```

Với `d_inner=1024, d_state=16, L=12, B=16` → ma trận ~**151 MB cho một layer**. Đó là lý do
`Avg Mamba + Head = 76.62 ms`.

### 15.2. ⚠️ ĐÍNH CHÍNH điều tôi nói ở tin trước

Tôi đã nói `cumsum`+`exp` là *"nguồn phụ thuộc N có tính cấu trúc"*. **Sai.** Kiểm chứng bằng numpy
(so với recurrence float64 làm chuẩn):

| L | sai số tương đối của bản `cumsum+exp` | ma trận L² | tuần tự |
|---|---|---|---|
| 4 | 2.431e-07 | 0.26 MB | 0.07 MB |
| 12 | 1.371e-06 | 2.36 MB | 0.20 MB |
| 16 | 8.762e-07 | 4.19 MB | 0.26 MB |
| 64 | 5.029e-06 | 67.11 MB | 1.05 MB |

Sai số chỉ ~1e-6 = **đúng mức float32** → **không phải** nguyên nhân N-collapse. Nó chỉ là vấn đề
**tốc độ + bộ nhớ**. N-collapse nằm ở **kiến trúc**: `mean(dim=1)` (phương sai ~1/N), `pos_embed`
(đóng góp `mean(PE[0:N])` phụ thuộc N), `BatchNorm1d` (stats fit ở 1 N), biên `conv1d` (3/L).

### 15.3. Đã thay bằng recurrence tuần tự

```python
a = torch.exp(W)                       # W < 0 -> a in (0,1), luôn ổn định
h = torch.zeros_like(V[:, 0])
h_list = []
for t in range(L):
    h = a[:, t] * h + V[:, t]
    h_list.append(h)
h = torch.stack(h_list, dim=1)
```

`h_t = exp(W_t)·h_{t-1} + V_t` ⟺ `h_i = Σ_{j≤i} exp(P_i − P_j) V_j` — **cùng công thức**.

| | Cũ (O(L²)) | Mới (tuần tự) |
|---|---|---|
| Bộ nhớ @L=16 | 4.19 MB | **0.26 MB** (16× nhỏ hơn) |
| `cumsum` | có (mất chính xác tăng theo L) | **không** |
| Rủi ro `inf*0=nan` | có (`masked_fill(-inf)`) | **không** |
| Tham số | — | **KHÔNG ĐỔI** → checkpoint cũ vẫn nạp |

**Quan trọng:** vì tham số không đổi, đây là **fix test được NGAY với checkpoint hiện tại, không cần
train lại**. Kỳ vọng `Avg Mamba + Head` giảm mạnh so với 76.62 ms.
